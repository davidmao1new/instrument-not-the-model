{
  "n_examined": 37,
  "n_confirmed": 37,
  "n_already_fixed": 0,
  "n_refuted": 0,
  "n_needs_human": 0,
  "by_root_cause": {
    "conflated-quantities": [
      "A3"
    ],
    "estimand-mislabeled": [
      "A4"
    ],
    "claim-outruns-evidence": [
      "STAT-3",
      "serving-bound-8.7",
      "token-direction-contradiction",
      "ratio-ranking-resurfaces",
      "decision-boundary-premise",
      "name-variance-priority",
      "batching-every-checkpoint",
      "RW-6",
      "SC-03",
      "SC-04",
      "SC-05",
      "STAT-2",
      "RW-2",
      "STAT-6"
    ],
    "selective-specification": [
      "STAT-5"
    ],
    "denominator-mismatch": [
      "CF-7"
    ],
    "unscoped-universal-negative": [
      "RW-3"
    ],
    "correction-not-propagated": [
      "RW-4",
      "R4"
    ],
    "novelty-overclaim-vs-cited-source": [
      "RW-5"
    ],
    "contrast-mismatch": [
      "R2"
    ],
    "claim-outruns-implementation": [
      "R3",
      "R6"
    ],
    "caption-misdescribes-estimator": [
      "table6-caption-posterior-medians"
    ],
    "two-intervals-one-estimand": [
      "STAT-3"
    ],
    "conjunction-over-different-model-subsets": [
      "STAT-6"
    ],
    "abstract-contradicts-body": [
      "A5"
    ],
    "caption-describes-wrong-statistic": [
      "A6"
    ],
    "count-scope-mismatch": [
      "A8"
    ],
    "interval-omits-denominator-uncertainty": [
      "STAT-1"
    ],
    "sign-stripped-interval": [
      "STAT-10"
    ],
    "hand-typed-stat-misstates-artifact": [
      "R5"
    ],
    "unit-mismatch": [
      "table3-saturated-denominator"
    ],
    "abstract-overstates-body": [
      "CF-10"
    ],
    "cited-source-mischaracterized": [
      "RW-9"
    ]
  },
  "confirmed": [
    {
      "id": "A3",
      "verdict": "CONFIRMED",
      "reason": "The sentence is still in the current build, verbatim (fresh PyMuPDF extraction of paper_instrument_validity_v3.pdf, p.11): \"there are 3 clusters, so a resample draws the same cluster every time with probability 0.11, and 602 of 20,000 resamples did exactly that and had to be discarded.\" The two numbers come from different bootstraps and cannot both describe one event. In data/instrument/name_length_effect.json under llama-3.1-8b-instruct.token_matched_first_name_clustered: same_cluster_draw_probability = 0.1111111111111111 and matched_minus_all.n_draws_degenerate = 602, n_draws = 19398, n_boot = 20000. 602/20000 = 3.01%, not 11% \u2014 a factor of 3.69. analyze_name_length.py settles which is which: line 306 sets atom = k_m ** (-(k_m - 1)) = 3^-2 = 0.111, and its comment (lines 302-305) attaches it to the MATCHED-ONLY bootstrap `bm`, which resamples the k_m = 3 matched clusters; those degenerate draws are never discarded, they stay in `bm` and pin the effect_token_matched percentile interval. The 602 comes from a different loop (lines 269-283): it resamples all k_all = 12 GRID clusters and increments `degen` when `sub = idx[matched[idx]]` is empty, i.e. when the draw contains none of the 3 matched clusters \u2014 the script's own comment says \"on Llama-3.1 they are 3% of draws precisely because only three of the twelve clusters carry a matched cell.\" I re-ran that exact loop against the raw data (load_effects/load_grid, seed 20260801, N_BOOT 20000) and reproduced degen = 602, kept = 19398, frac = 0.0301, against the closed form (9/12)^12 = 0.03168 (expected 633.5). So \"did exactly that\" is false: the discarded draws are draws with NO matched cluster, not draws with the SAME cluster three times. The builder's own comment at build_paper_v3.py:2190-2196 is where the conflation is asserted (\"which is the probability of a wholly degenerate draw\"); it corrects an older, different error (\"any two draws coincide\") but leaves the 0.11-equals-602 identity in place.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, replace the fragment on lines 2197-2201 so the two quantities are no longer asserted to be one. Smallest working replacement: + f\"{_cl['n_matched_clusters']} matched clusters among the grid's {_cl['n_grid_clusters']}, so the matched arm alone puts probability \" + f\"{_cl['same_cluster_draw_probability']:.2f} on drawing the same cluster every time, and {_mm['n_draws_degenerate']:,} of \" + f\"{_cl['n_boot']:,} joint resamples ({_mm['n_draws_degenerate']/_cl['n_boot']:.1%}, against (9/12)^12 = 3.2 % expected) contained no matched cluster at all \" \"and had to be discarded.\" Also delete or rewrite the comment at lines 2190-2196, whose clause \"which is the probability of a wholly degenerate draw\" is the assertion that produced the error. No artifact field needs to change \u2014 same_cluster_draw_probability and n_draws_degenerate are both correct for what they measure; only the prose that fuses them is wrong.",
      "evidence_command": "cd \"paper-a/src\" && C:/research-toolchain/venv/Scripts/python.exe -c \"import sys,numpy as np; sys.path.insert(0,'.'); import analyze_name_length as A; m='llama-3.1-8b-instruct'; eff,_=A.load_effects(m); y,first,last,matched,delta=A.load_grid(m,eff); cl=sorted(set(first.tolist())); ga=[np.flatnonzero(first==c) for c in cl]; k=len(ga); rng=np.random.default_rng(A.CLUSTER_SEED); d=0\nfor _ in range(A.N_BOOT):\n    idx=np.concatenate([ga[i] for i in rng.integers(0,k,k)])\n    d += len(idx[matched[idx]])==0\nprint('degen',d,'frac',d/A.N_BOOT,'theory',(9/12)**12,'atom 3^-2',3**-2)\"  # prints degen 602 frac 0.0301 theory 0.03168 atom 0.1111; see also build_paper_v3.py:2197-2201 and analyze_name_length.py:269-283,302-306",
      "root_cause": "conflated-quantities",
      "severity": "major"
    },
    {
      "id": "A4",
      "verdict": "CONFIRMED",
      "reason": "The sentence is still in the current build (fresh PyMuPDF extract of paper_instrument_validity_v3.pdf, Table 6 caption, p.10). The first three columns are built at build_paper_v3.py:1791 from name_variance.json -> averaging_asymmetry_with_replacement[\"ratio_name_to_wording\"], which fit_name_variance.py:172 defines as c[\"beta_sd\"] / sigma_variant_paired. The numerator beta_sd (fit_name_variance.py:451) is e.std(ddof=1) over n_boot=4000 draws of counterfactual_draws(replace=True) -- a CLUSTER BOOTSTRAP resampling whole matched first-name pairs from the observed 12-pair grid, not a posterior quantity. I reproduced it exactly from names_*.jsonl with the recorded seed np.random.default_rng(20260729): llama-2-7b-chat beta_sd = 0.0766474 / 0.05406007 / 0.04308794 at k = 3/6/9, giving 1.2086 / 0.8524 / 0.6794, i.e. the printed 1.21x / 0.85x / 0.68x. Only the DENOMINATOR is a posterior median (fit_name_variance.py:496 takes index [1] of delta_stability/variance_components.json[model][\"all\"][\"sigma_variant\"] = 0.06341742724180222 for llama-2). So \"ratios of posterior MEDIANS\" misdescribes the numerator. The second sentence is substantially accurate -- names/asymmetry_uncertainty.json records _bound_type \"corner bound from marginal 2.5/50/97.5 quantiles\" of both sigmas -- though that column's own point estimate (est 1.1844/0.8375/0.6838) is a different estimator from the printed cells, so the bound is not an interval around the numbers beside it.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, lines 1811-1812, replace the two caption fragments   \"The first three columns are ratios of posterior \"   \"MEDIANS. The last propagates both posteriors and is a \"   with   \"The first three columns divide the SD of the published effect over \"   \"4,000 cluster-bootstrap draws of k matched name pairs by the \"   \"posterior median of the between-wording SD. The last propagates \"   \"both posteriors and is a \"   leaving the rest of the caption unchanged. (Optionally add, one clause later, that the bound column is centred on its own posterior-median estimator rather than on the printed cells.)",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json;d=json.load(open(r'paper-a/data/names/name_variance.json'));m=d['llama-2-7b-chat'];print(m['averaging_asymmetry_with_replacement']['draw_9_per_race']);print('n_sim',m['counterfactual_with_replacement']['draw_9_per_race']['n_sim'],'mode',m['counterfactual_with_replacement']['draw_9_per_race']['draw_mode'])\"  -- then read paper-a/src/fit_name_variance.py:170-173 and :451 (beta_sd=float(e.std(ddof=1)) inside counterfactual_draws(n_boot=4000)), and paper-a/src/build_paper_v3.py:1811",
      "root_cause": "estimand-mislabeled",
      "severity": "major"
    },
    {
      "id": "STAT-3",
      "verdict": "CONFIRMED",
      "reason": "Fresh PyMuPDF extraction of the current paper_instrument_validity_v3.pdf still carries the unqualified universal quantifier at all three sites: \u00a75.2 \"the shift is held within 8.7 % of the effect on every checkpoint we could manipulate\"; \u00a79 item 7 \"bounded within 8.7 % of it on the checkpoints where we could manipulate them\"; Conclusion \"bounded within 8.7 % of the effect on every checkpoint where we could manipulate them\". But build_paper_v3.py lines 559-563 filter the max to `_m in survives` (`if (_m.startswith(\"_\") or _m not in ps or _m not in survives ...): continue`), and survives (line 317) is the 2 of 4 Study 2 models whose log-odds CI excludes zero \u2014 llama-2-7b-chat and llama-3.1-8b-instruct. Concurrency was manipulated on all four checkpoints (replicate_analysis.json has effect_difference for all four); cache on two (cache_residual.json). Computing the same ratio max|CI| / |lo[m].est| for the excluded checkpoints gives 28.8 % (mistral-7b-instruct-v0.1, |est| 0.0326, CI upper 0.009389) and 16.2 % (mistral-7b-instruct-v0.3, |est| 0.0853, CI lower -0.013860); the true max over every manipulated checkpoint is 28.8 %, not 8.7 %. I verified the artifacts against raw JSONL: effect_conc1/effect_conc4 reproduce to 8 dp as the mean paired white_margin-black_margin over rep_*.jsonl (e.g. mistral v0.1: -0.05778803 / -0.05867577), and a 4000-draw name-pair bootstrap on rep_mistral-7b-instruct-v0.1.jsonl reproduces the stored CI ([-0.00784, 0.00918] vs stored [-0.00769, 0.00939]). So the data are sound and the exclusion is a deliberate coded policy (comment at lines 555-560 about dividing by an effect that covers zero) \u2014 the defect is purely that the prose quantifier claims coverage the number does not have. The paper qualifies this exact way everywhere else (abstract \"where the effect is distinguishable from zero\"; \u00a75.1 \"on the models where the effect is identified. Where it is not identified the ratio is larger still\"), which makes the serving sentences the outlier. No footnote or nearby sentence in \u00a75.2, \u00a79 or the Conclusion restores the restriction.",
      "minimal_fix": "Restrict the quantifier at the three prose sites that interpolate `_serving_bound`; leave the computation at lines 549-573 unchanged (widening the max to 28.8 % would quote a ratio against a denominator that covers zero, which \u00a74.5 exists to forbid). In paper-a/src/build_paper_v3.py: line 3400-3401, replace \"of the effect on every checkpoint we could manipulate\" with \"of the effect on the two checkpoints whose effect is distinguishable from zero\"; line 4287, replace \"on the checkpoints where we could manipulate them\" with \"on the two checkpoints whose effect is distinguishable from zero\"; line 4664-4665, replace \"of the effect on every checkpoint where we could manipulate them\" with \"of the effect on the two checkpoints whose effect is distinguishable from zero\". Optionally (author's call, not required for correctness) append the excluded ratios once in \u00a75.2: \"on the other two the same intervals reach 16 % and 29 % of their own unidentified effects\".",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;D=r'paper-a/data/';s2=json.load(open(D+'delta_stability/study2_v2.json',encoding='utf-8'));lo={m:v['overall']['logodds'] for m,v in s2.items()};surv=[m for m in lo if lo[m]['ci'][0]*lo[m]['ci'][1]>0];[print(f,m,'survives' if m in surv else 'EXCLUDED',round(max(abs(x) for x in v['effect_difference']['ci'])/abs(lo[m]['est'])*100,1)) for f in ('replicate_analysis.json','cache_residual.json') for m,v in json.load(open(D+'replicate/'+f,encoding='utf-8')).items() if not m.startswith('_') and v.get('effect_difference')]\"  # prints 8.7/5.6 survives, 28.8/16.2/4.1 EXCLUDED; compare build_paper_v3.py:561 and the PDF text at build_paper_v3.py:3399, :4286, :4663",
      "root_cause": "claim-outruns-evidence",
      "severity": "major"
    },
    {
      "id": "STAT-5",
      "verdict": "CONFIRMED",
      "reason": "The current PDF (fresh PyMuPDF extract, \u00a74.1) reports \"the posterior probability that the null arm exceeds the semantic arm ranges from 0.12 to 0.83, and every difference interval includes zero.\" build_paper_v3.py:1532 sources that range from arm_contrast.json's [\"all\"] branch only (per-model 0.123, 0.835, 0.349, 0.351). The artifact also stores a [\"dropN1\"] branch (0.048, 0.883, 0.287, 0.403 -> range 0.05 to 0.88), and greps for `arm[`, `dropN1`, `prob_null_exceeds` across the builder return only the path line (46) and line 1532 \u2014 the second fit is never read, and greps for \"N1\", \"0.88\", \"dropped\" over the extracted PDF text show no mention of it anywhere in the paper. fit_arm_contrast.py:35-38 states: \"Variant N1 is byte-identical to S1 by construction. Its true variant effect is therefore not an independent draw from the null arm's distribution, it is the same draw as S1's. Including it makes the null arm look slightly less dispersed than it is. The fit is reported both ways.\" The paper reports only the branch its own analysis code flags as biased, and the bias direction (mixing a semantic-arm draw into the null arm) pulls the difference toward zero, i.e. toward the paper's stated conclusion. The omission is material: under dropN1 llama-2-7b-chat has P(null>sem)=0.048, i.e. 95.2% one-sided in the opposite direction, which strains the blanket \"the two arms cannot be distinguished on any model\" \u2014 though all four difference intervals still include zero under both branches, so the headline conclusion itself survives. Recomputation from the raw delta_*.jsonl (dedup on model/variant/template/pair, both margins non-null) gives 432 rows / 12 variants per model and 396 / 11 with N1 dropped, matching n and n_variants in both JSON branches, so the artifact is consistent with the data and the defect is purely the unreported second specification.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, at line 1532 add a second read of the already-computed branch: `pnd = [arm[m][\"dropN1\"][\"prob_null_exceeds_semantic\"] for m in am]`, then in the P(...) sentence that follows replace `f\"{fmt(min(pn), 2)} to {fmt(max(pn), 2)}, and every difference interval includes zero.\"` with `f\"{fmt(min(pn), 2)} to {fmt(max(pn), 2)} with all twelve wordings and {fmt(min(pnd), 2)} to {fmt(max(pnd), 2)} with N1 dropped \u2014 N1 being byte-identical to S1 and therefore not an independent draw from the null arm \u2014 and every difference interval includes zero on both.\"` No new computation is needed; arm_contrast.json already contains the dropN1 fit.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json; d=json.load(open(r'paper-a/data/delta_stability/arm_contrast.json')); [print(t, '%.3f-%.3f'%(min(p),max(p)), ['%.3f'%x for x in p]) for t in ('all','dropN1') for p in [[d[m][t]['prob_null_exceeds_semantic'] for m in sorted(d)]]]\"  # prints all 0.123-0.835 vs dropN1 0.048-0.883; compare build_paper_v3.py:1532 which reads only [\"all\"], and fit_arm_contrast.py:35-38",
      "severity": "major",
      "root_cause": "selective-specification"
    },
    {
      "id": "serving-bound-8.7",
      "verdict": "CONFIRMED",
      "reason": "The current PDF still says, in three places, that the shift is bounded within 8.7 % \"on every checkpoint where we could manipulate them\" (Conclusion, built at build_paper_v3.py:4663-4664), \"on the checkpoints where we could manipulate them\" (\u00a79 item 7, line 4286-4287), and \"on every checkpoint we could manipulate\" (\u00a75.2, line 3399-3400). But _serving_bound (lines 549-573) skips any model not in `survives` (line 561), and `survives` (line 317) is the two checkpoints whose Study 2 log-odds CI excludes zero: llama-2-7b-chat and llama-3.1-8b-instruct. data/replicate/replicate_analysis.json carries an effect_difference for all FOUR checkpoints, so concurrency was in fact manipulated on four. Recomputing max|CI endpoint| / |Study 2 log-odds| per artifact row: llama-2-7b 8.7 %, llama-3.1 5.6 % (both in survives), mistral-v0.1 28.8 %, mistral-v0.3 16.2 % (both dropped); cache_residual.json gives 8.0 % and 4.1 %. The true max over every checkpoint manipulated is 28.8 %, not 8.7 %. No narrower reading saves it: restricting to checkpoints where BOTH concurrency and cache were manipulated gives {llama-2-7b, mistral-v0.3}, max 16.2 %. The restriction to identified denominators is itself defensible (builder comment lines 557-560, consistent with \u00a74.5/Table 10 and with the abstract's own \"unbounded where the effect is not distinguishable from zero\" language) \u2014 the defect is the universal quantifier in the prose, not the 8.7 % value.",
      "minimal_fix": "Replace the coverage phrase at the three call sites so it names the restriction the code enforces. In paper-a/src/build_paper_v3.py: line 4664 \"effect on every checkpoint where we could manipulate them\" -> \"effect on the two checkpoints whose effect is distinguishable from zero\"; line 4287 \"on the checkpoints where we could manipulate them\" -> \"on the checkpoints whose effect is distinguishable from zero\"; line 3400 \"checkpoint we could manipulate\" -> \"checkpoint whose effect is distinguishable from zero\". (No artifact field changes; _serving_bound = 8.7 % is correct for that set.)",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json,pathlib; D=pathlib.Path(r'paper-a/data'); lo={m:v['overall']['logodds'] for m,v in json.loads((D/'delta_stability'/'study2_v2.json').read_text()).items() if isinstance(v,dict) and 'overall' in v}; [print(f,m,round(max(map(abs,v['effect_difference']['ci']))/abs(lo[m]['est'])*100,1),'survives' if lo[m]['ci'][0]*lo[m]['ci'][1]>0 else 'NOT-identified') for f in ('replicate_analysis.json','cache_residual.json') for m,v in json.loads((D/'replicate'/f).read_text()).items() if isinstance(v,dict) and 'effect_difference' in v]\"  # then compare against build_paper_v3.py:561 (survives filter) and :4663-4664 / :4286-4287 / :3399-3400 (prose)",
      "root_cause": "claim-outruns-evidence",
      "severity": "major"
    },
    {
      "id": "token-direction-contradiction",
      "verdict": "CONFIRMED",
      "severity": "major",
      "root_cause": "claim-outruns-evidence",
      "reason": "Fresh PyMuPDF extraction of the current PDF still contains both flat directional claims: \u00a71.2 ends \"...a shift not itself separable from zero on the name-pair resampling unit - and the direction of the bias is to understate rather than inflate\", and \u00a74.4's \"What is new here, and what is not\" paragraph ends \"...that the imbalance predicts the reported effect on a hiring outcome, and that its direction is to understate.\" Both are contradicted inside the same build by the abstract (\"The direction of the four point estimates favours masking over manufacturing, and no more than that: on the two models whose baseline effect can carry a ratio the split is one each way\") and by \u00a74.4's own body paragraph (\"...and on Llama-2-7B-chat it did the reverse: of the two models whose baseline effect can carry a ratio at all, one moves away from zero and one moves toward it, so the panel does not settle the direction\"). The artifact backs the hedged version, not the flat one: in data/instrument/name_length_effect.json, token_matched_first_name_clustered.growth_ratio.interpretable is true only for llama-2-7b-chat and llama-3.1-8b-instruct; llama-3.1 has est +0.6056 (away from zero) while llama-2-7b has est -0.1046, with effect_all_pairs +0.18971 shrinking to effect_token_matched +0.16987 - i.e. moving TOWARD zero, the confound inflating rather than masking. The builder proves this was a fix applied to only two of four sites: build_paper_v3.py:376-382 carries the comment \"among the two that ARE interpretable the split is one each way, and Llama-2-7B moves TOWARD zero, meaning the confound inflated rather than masked on that model. The paper asserted the causal direction flatly\", and defines _mask_interp (line 383) and _mask_interp_away (line 384) which are then never referenced anywhere in the file, while _mask_toward is consumed only at line 2123 (\u00a74.4 body) and the abstract hedge is hard-coded at lines 727-729. The stale flat text survives at line 1016 (\u00a71.2) and line 2338 (\u00a74.4 \"What is new here\"). The \"three of them / three of four\" count itself is correct (3 of 4 models move away from zero) and is not the defect; only the trailing causal-direction clause is.",
      "minimal_fix": "Two deletions in paper-a/src/build_paper_v3.py, then rebuild. (1) Lines 1015-1016: change the fragment \"separable from zero on the name-pair resampling unit - and the \" / \"direction of the bias is to understate rather than inflate\" to end at \"separable from zero on the name-pair resampling unit\" - i.e. drop the trailing clause so \u00a71.2 stops at the shift and its interval. If \u00a71.2 should keep a direction statement, replace the deleted clause with the abstract's own wording, which is already computed: \", though on the two models whose baseline effect can carry a ratio the split is one each way\". (2) Line 2338: change \"on a hiring outcome, and that its direction is to understate.)\" to \"on a hiring outcome. Which way it biases the estimate these four checkpoints do not settle.)\" No artifact field needs to change - name_length_effect.json already flags interpretable=false on the two Mistral checkpoints and already records growth_ratio.est = -0.1046 for llama-2-7b-chat. Optionally wire the already-computed but unused _mask_interp / _mask_interp_away (lines 383-387) into the \u00a71.2 replacement so the two sites can never drift from the abstract again.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz,re,json,io,sys; sys.stdout=io.TextIOWrapper(sys.stdout.buffer,encoding='utf-8',errors='replace'); t=chr(10).join(p.get_text() for p in fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf')); [print('PDF@',m.start(),repr(t[max(0,m.start()-150):m.start()+80])) for m in re.finditer(r'direction of the bias|its direction is to understate|does not settle the direction|split is one each way',t)]; d=json.load(open(r'paper-a/data/instrument/name_length_effect.json',encoding='utf-8')); [print(k, 'all=',round(d[k]['token_matched_first_name_clustered']['effect_all_pairs']['est'],5), 'tm=',round(d[k]['token_matched_first_name_clustered']['effect_token_matched']['est'],5), 'growth=',d[k]['token_matched_first_name_clustered']['growth_ratio']) for k in d if not k.startswith('_')]\"  # then: build_paper_v3.py lines 727-729 and 2123-2127 (hedged) vs lines 1015-1016 and 2338 (flat)"
    },
    {
      "id": "ratio-ranking-resurfaces",
      "verdict": "CONFIRMED",
      "severity": "major",
      "root_cause": "claim-outruns-evidence",
      "reason": "Fresh text from the current PDF (page 27, \u00a710.2) still reads \"...and on the 2 where the effect is identified the dispersion-to-effect ratio is LARGER than on the open-weight panel, not smaller.\" That is an unhedged ranking of the two panels. \u00a74.7 of the same build (page 15) explicitly withdraws it twice: \"Neither ratio is pinned tightly enough to rank the two panels, and we do not\" and \"not one interval separates from its counterpart. The ordering is a direction, not a result, and an earlier draft of this paper stated it as a result three times - including in the abstract - while this section disclaimed it. The disclaimer was right.\" The artifact agrees with \u00a74.7, not \u00a710.2: data/reference/ratio_intervals.json field comparison has intervals_disjoint=false, worst_case_overlap=-0.0422, ranking_is_supported=false, and _verdict=\"the point estimates order one way on every checkpoint, but the intervals overlap, so the ordering is a direction and not a finding; the paper must not claim the frontier ratio is larger\". The raw CIs confirm the overlap: frontier gpt-4o-mini [0.376, 1.197] and gpt-4o [0.450, 2.785] against local llama-2-7b-chat [0.241, 0.656] and llama-3.1-8b-instruct [0.178, 0.497]. The abstract was already repaired in this build (it now says only \"give a dispersion-to-effect ratio of 0.54 to 0.64 ... and what moves between the panels is the denominator\"), so \u00a710.2 is the one surviving site. \u00a74.7.1's \"the same ordering as before, on numbers that are now comparable\" does not rescue it: no interval is computed for the floor-corrected ratios, so the ranking still has no interval behind it. The sentence is generated at build_paper_v3.py:4479-4484 in the _front_note f-string; the summary fields it interpolates (n_models=4, n_identified=2 from data/frontier/frontier_margin_analysis.json) are correct - only the comparative clause is unsupported.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, replace lines 4483-4484 \u2014 currently `f\"identified the dispersion-to-effect ratio is LARGER than on the \"` / `f\"open-weight panel, not smaller.\")` \u2014 with `f\"identified the dispersion-to-effect ratio POINT ESTIMATES are \"` / `f\"higher than on the open-weight panel, though no interval \"` / `f\"separates the two panels (\u00a74.7).\")`. No other line changes; \u00a74.7 and the abstract already read correctly.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz,json; t=''.join(p.get_text() for p in fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf')); i=t.find('answers most of the rest'); print(t[i-30:i+330]); print(json.dumps(json.load(open(r'paper-a/data/reference/ratio_intervals.json',encoding='utf-8'))['comparison'],indent=1))\"  # also: build_paper_v3.py:4483"
    },
    {
      "id": "decision-boundary-premise",
      "verdict": "CONFIRMED",
      "reason": "The sentence is present verbatim in the current PDF (p. 2, \"Why hiring, and why this design.\") and in build_paper_v3.py lines 968-971. The premise \"Hiring also puts the model near a decision boundary\" is false on half the panel \u00a75.2 is built from: noise_vs_probability.json field models[*].n_in_band (band 0.2-0.8) is 24/72 for llama-2-7b-chat and mistral-7b-instruct-v0.1 but 0/72 for llama-3.1-8b-instruct and mistral-7b-instruct-v0.3, and summary.n_models_with_no_cell_in_band = 2. Recomputing from the raw rep_*.jsonl with the same cell definition as analyze_noise_vs_probability.py::cells reproduces 24/0/24/0 exactly; mistral v0.3's cells are exactly 0.0 or 1.0 at every quantile. The paper contradicts its own intro downstream: \u00a75.2 states \"The other 2 have no cell in that band at all\" and warns that \"a floor measured on a saturated task does not license a claim about a task near the boundary\", and \u00a76.2 (builder line 3538) states \"The four models do not share an operating point\", with saturated_frac in data/delta_stability/study2_v2.json running 0.0% to 99.7%. The unqualified \"the model\" therefore generalises a two-of-four result to the whole panel.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, replace lines 968-969 \u2014 \"it should do to the other, and cancel. Hiring also puts the model near \" / \"a decision boundary, and \u00a75.2 shows that is where the \" \u2014 with: \"it should do to the other, and cancel. Hiring puts SOME cells near \" / \"a decision boundary \u2014 a third of the replicate cells on two of the four \" / \"checkpoints, none on the other two \u2014 and \u00a75.2 shows that is where the \". Nothing else in the sentence changes; the trailing clause about p = 0.5 remains correct (rho positive on all 4 models). The numbers are also available programmatically from the already-loaded funoise artifact (models[*].n_in_band / n_cells, summary.n_models_with_no_cell_in_band) if the author prefers interpolation over literals.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -X utf8 -c \"import json;d=json.load(open(r'paper-a/data/replicate/noise_vs_probability.json',encoding='utf-8'));[print(m,v['n_in_band'],'/',v['n_cells']) for m,v in d['models'].items()]\" ; sed -n '964,976p' \"paper-a/src/build_paper_v3.py\"",
      "severity": "major",
      "root_cause": "claim-outruns-evidence"
    },
    {
      "id": "name-variance-priority",
      "verdict": "CONFIRMED",
      "reason": "Fresh PyMuPDF extraction of the current build still carries the unqualified priority claim in \u00a71.2: \"The first estimate of name-draw variance in an LLM audit.\" (build_paper_v3.py:1002). The paper's own coded artifact refutes it: reporting_practice_matrix.json, studies[1] \"Armstrong et al. 2024\", cell name_sensitivity_reported.value reads \"Name is modelled as a random effect and its variance component is printed in every table (Rating: Name intercept variance 0.016, SD 0.127; Interview: 0, 0; Hiring: 0.028, 0.169; Study 2 experience model: 17.34, 4.164)\". I checked that against the primary source rather than trusting the coding: lit/text/armstrong_etal_2024_silicon_ceiling.txt contains \"we treat the name as a random effect in our model\" and the printed block \"Random Effects Name Variance Std. Dev. Name (Intercept) 0.016 0.127\" with race and gender as fixed effects \u2014 a published within-cell between-name variance estimate in an LLM hiring audit, in the paper's own 13-audit panel. The second audit, studies[6] Hoffstedde et al. 2026, fits \"resume identity, model, and name pair as crossed random intercepts\" (verified in lit/text/hoffstedde_etal_2026_gender_bias_japanese_hiring.txt) but prints no variance components, only a boundary warning \u2014 so the finding's \"two ... estimates\" slightly overstates; one published estimate is enough to break \"first\". The paper never surfaces either fact: the flattened PDF text has zero hits for \"random effect\"/\"random intercept\" outside the paper's own crossed random-effects fit; Armstrong appears only in Table 18's checkpoint-pinning and resampling-unit rows and in the references, Hoffstedde only in the references. Collateral: the bullet's second sentence, \"which a list of hand-picked full names cannot do\", is also too strong \u2014 Armstrong's hand-picked full-name list did yield a within-cell between-name SD separate from the race fixed effect; what the grid uniquely adds is separating first-name from surname. The claim is an outlier against the paper's own novelty framing elsewhere in \u00a78, which explicitly rests novelty on the four choices no surveyed audit reports (name-draw sensitivity is not among them).",
      "minimal_fix": "Edit build_paper_v3.py lines 1002-1005: retitle the bullet and add one acknowledging clause, e.g. P(\"<b>The first name-draw variance separately identified from the between-race effect.</b> Armstrong et al. (2024) print a name random-effect variance component and Hoffstedde et al. (2026) fit name pair as a crossed random intercept without reporting one; a factorial grid crossing first names with surnames is what makes the within-race between-first-name standard deviation separately estimable from the between-race effect, and separates the first-name component from the surname, which a list of hand-picked full names cannot do.\"). No artifact change is needed \u2014 reporting_practice_matrix.json already records the correct facts.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz,json,re;t=''.join(p.get_text() for p in fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf'));print('CLAIM IN CURRENT BUILD:', 'The first estimate of name-draw variance in an LLM audit' in re.sub(r'\\s+',' ',t));print('ARTIFACT:', json.load(open(r'paper-a/data/reference/reporting_practice_matrix.json',encoding='utf-8'))['studies'][1]['cells']['name_sensitivity_reported']['value'])\"  # then: grep -n \"Name (Intercept)\" \"lit/text/armstrong_etal_2024_silicon_ceiling.txt\" ; claim source at paper-a/src/build_paper_v3.py:1002",
      "severity": "major",
      "root_cause": "claim-outruns-evidence"
    },
    {
      "id": "batching-every-checkpoint",
      "verdict": "CONFIRMED",
      "reason": "The sentence is present verbatim in the current build (p. 3, \u00a71 bullet \"A complete account of the apparent nondeterminism\"), freshly extracted from paper_instrument_validity_v3.pdf. \"The panel\" is defined in \u00a71.1 and \u00a73 as six open-weight checkpoints (Llama-2-7B-chat, Llama-2-13B-chat, Llama-3.1-8B-Instruct, Mistral-7B-Instruct v0.1, v0.3, Mistral-7B v0.1 base), and nothing in the bullet or its context narrows it. But the batching manipulation exists on only four: paper-a/data/replicate/ holds rep_*.jsonl for llama-2-7b-chat, llama-3.1-8b-instruct, mistral-7b-instruct-v0.1 and mistral-7b-instruct-v0.3 only, each with 180 records at concurrency 4 and 180 at concurrency 1; llama-2-13b-chat and mistral-7b-v0.1-base have no replicate file and appear in no concurrency-varying artifact anywhere under data/. replicate_analysis.json has exactly four model keys, and its _verdict fields (frac_identical_conc4 0.4792, conc1 0.9375, sd_ratio 8.486) are the very numbers the paper prints for Study 8 (47.9 %, 93.8 %, 8.5x). The paper contradicts itself elsewhere: \u00a75.2 says the shift is held \"within 8.7 % of the effect on every checkpoint we could manipulate\", \u00a75.2 reports cross-session disagreement \"on three of the four models\", Table 15 lists two models, and \u00a79 register item 7 says \"the checkpoints where we could manipulate them\". The finding's second clause is correct and should be left alone: cache_residual.json covers exactly two models (llama-2-7b-chat, mistral-7b-instruct-v0.3), which are exactly the two with nocache_session2_*.jsonl. The prose is hardcoded, not artifact-derived, so no rebuild would have caught it.",
      "minimal_fix": "In paper-a\\src\\build_paper_v3.py, lines 1041-1042, change the string fragment \"Batching is manipulated on every \" / \"checkpoint in the panel;\" to \"Batching is manipulated on four of the six \" / \"checkpoints;\" (leave the rest of the sentence, including the cache-residency clause and the \"complete\" caveat, unchanged), then rebuild.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json,glob,os,collections; [print(os.path.basename(f), collections.Counter(json.loads(l)['concurrency'] for l in open(f,encoding='utf-8') if l.strip())) for f in sorted(glob.glob(r'paper-a/data/replicate/rep_*.jsonl'))]\"  # 4 files, not 6; compare to build_paper_v3.py:1041-1042 and the six-checkpoint panel in build output p.4 \u00a73",
      "severity": "major",
      "root_cause": "claim-outruns-evidence"
    },
    {
      "id": "CF-7",
      "verdict": "CONFIRMED",
      "reason": "Fresh PyMuPDF extraction of paper_instrument_validity_v3.pdf (p.17) still reads \"Table 15. The same 36 cells, five repeats each, under three serving configurations.\" with no denominator, and the table lists only Llama-2-7B and Mistral v0.3. The immediately preceding Study 8 sentence reports \"agreement rises from 47.9 % at concurrency four to 93.8 % at concurrency one\", which build_paper_v3.py:3322-3323 pulls from data/replicate/replicate_analysis.json `_verdict.frac_identical_conc4 = 0.479166` / `frac_identical_conc1 = 0.9375` \u2014 a mean over FOUR checkpoints. Table 15 is built from data/replicate/cache_residual.json (build_paper_v3.py:3327-3348), which contains only TWO models. Recomputed from the raw rep_*.jsonl (36 cells x 5 repeats per model): llama-2-7b 63.89/91.67, llama-3.1-8b 13.89/97.22, mistral-v0.1 63.89/91.67, mistral-v0.3 50.00/94.44; 4-model mean = 47.92/93.75 (matches the text), 2-model mean = 56.94/93.06 (what the table averages to). A reader checking 47.9 % against the adjacent table gets 56.9 %. The two dropped checkpoints appear nowhere in the paper (\"97.2\" occurs zero times in the PDF text; \"13.9\" only in unrelated contexts), and the dropped Llama-3.1 is the worst checkpoint (13.9 %), so the table understates the failure the sentence reports. The denominator switches again in the next paragraph: Study 9's \"93.1 % to 100.0 %\" is the 2-model cache_residual `_verdict` (0.930555), so adjacent sentences use different panels undisclosed. Table 15 is never cross-referenced from body text.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, extend the Table 15 caption string at lines 3344-3348 so the caption itself carries both denominators \u2014 append one sentence such as: \" The concurrency contrast is shown here on the two checkpoints where the cache arm was also run; the 47.9 % and 93.8 % figures in the text average Study 8's four checkpoints, of which Llama-3.1-8B (13.9 % to 97.2 %) and Mistral v0.1 (63.9 % to 91.7 %) are not repeated here.\" Equivalent two-clause alternative: add \"on all four checkpoints\" to the Study 8 sentence at build_paper_v3.py:3320-3321 and \"on two of those four checkpoints\" to the caption at line 3344. No artifact field needs to change \u2014 replicate_analysis.json and cache_residual.json are both correct; only the prose omits their differing scope.",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json,glob,numpy as np;from collections import defaultdict;import os;os.chdir(r'paper-a/data/replicate');rows=[json.loads(l) for f in sorted(glob.glob('rep_*.jsonl')) for l in open(f,encoding='utf-8') if l.strip()];ms=sorted({r['model'] for r in rows});f=lambda m,c:(lambda F:np.mean([len(set(v))==1 for v in F]))([v for v in (lambda d:[d[k] for k in d])((lambda d:[d[(r['template'],r['pair'])].append((r['white_margin'],r['black_margin'])) for r in rows if r['model']==m and r['concurrency']==c] and d)(defaultdict(list))) if len(v)>=2]);print('per-model conc4:',[(m,round(f(m,4),4)) for m in ms]);print('4-model mean conc4:',round(np.mean([f(m,4) for m in ms]),4),'text says 47.9%');print('table models:',list(k for k in json.load(open('cache_residual.json')) if not k.startswith('_')));print('json _verdict:',json.load(open('replicate_analysis.json'))['_verdict']['frac_identical_conc4'])\"  # then see build_paper_v3.py:3322 (4-model source) vs build_paper_v3.py:3327-3348 (2-model table+caption)",
      "root_cause": "denominator-mismatch",
      "severity": "major"
    },
    {
      "id": "RW-3",
      "verdict": "CONFIRMED",
      "reason": "Both sentences are still in the CURRENT build (fresh PyMuPDF extraction of paper_instrument_validity_v3.pdf, p.25 \u00a79 item 5: \"...on half our panel that is the situation (\u00a74.3). No audit we surveyed does this.\"; p.28 \u00a710.3: \"No LLM audit we surveyed runs this check, and a null reported without it is not evidence of fairness.\"). Both are universal negatives over the surveyed panel, and the panel includes Tan et al. 2026 \u2014 reporting_practice_matrix.json lists it under llm_hiring_audits (13 studies, the same 13 Table 18 counts). Tan et al. run exactly the check item 5's first branch names (\"by probing it directly\"): lit/text/tan_etal_2026_small_changes.txt:524 \u00a73.6 \"To validate that injected markers are effective demographic proxies, we conduct recoverability experiments: LLMs are prompted to classify the gender and ethnicity of candidates based solely on resume content\", with results at :553 \"ethnicity classification achieves macro-F1 scores ranging from 0.925 to 0.997, while gender classification achieves F1 scores of 0.911 to 0.980. These results confirm that the injected markers function as effective demographic proxies\", plus a 32-annotator human validation of the same association (gender F1 0.96, ethnicity F1 1.00). The paper's own matrix entry for Tan records this (\"the marker-category ablation that IS reported measures only demographic RECOVERABILITY\"), so the artifact already knows the check exists. Two further points make this a defect rather than a defensible shorthand. (1) The claim has no artifact backing: reporting_practice_matrix.json has 22 count fields and none is a manipulation check, so item 5 is the only \u00a79 item that asserts a panel-wide count without printing a denominator from matrix['counts'] \u2014 items 1, 4, 6, 7 all print \"X of N\". (2) The paper demonstrates elsewhere that it knows how to scope this kind of statement (\u00a78.1: \"Of the 10 LLM audits to which the same check applies, none reports anything at the level of the individual name\") and explicitly credits a precedent it does not want to claim (\u00a72: \"Seshadri et al. do run a null-edit control, and Table 18 credits them for it\"). By the paper's own standard \u2014 \"a false claim about someone else's reporting would be the same kind of error this paper is about\" \u2014 an uncredited, unscoped universal over a panel member that does run the check is the error. The narrow reading that saves the sentences (Tan has no name list, so the item is not-applicable and excluded per the matrix's counting rule) is available for \u00a710.3, whose antecedent is the per-name correlation, but not for item 5, which offers direct probing as a sufficient alternative.",
      "minimal_fix": "Two one-clause edits in paper-a/src/build_paper_v3.py, no artifact or figure change. Line 4264: replace \"No audit we surveyed does this.\" with \"No audit we surveyed does this on a name list. Tan et al. (2026), whose stimulus is sociocultural markers rather than names, run the analogous direct probe and report demographic recoverability of 0.925 to 0.997 macro-F1 for ethnicity and 0.911 to 0.980 for gender.\" Line 4546: replace \"No LLM audit we surveyed runs this check, and a null reported \" with \"No LLM audit we surveyed runs this check on a name list, and a null reported \".",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz;d=fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf');t=''.join(p.get_text() for p in d);print('ITEM5:', 'No audit we surveyed does this' in t);print('S10.3:', 'No LLM audit we surveyed runs this check' in t)\" ; then compare against lit/text/tan_etal_2026_small_changes.txt:524 and :553, and build_paper_v3.py:4264 and :4546",
      "root_cause": "unscoped-universal-negative",
      "severity": "major"
    },
    {
      "id": "RW-4",
      "verdict": "CONFIRMED",
      "severity": "major",
      "root_cause": "correction-not-propagated",
      "reason": "Fresh PyMuPDF extraction of paper_instrument_validity_v3.pdf still has the defective \u00a72 sentence (build_paper_v3.py:1136-1141): \"most individual format features do not independently predict performance ... with two exceptions: separators and the number format used in enumerations are the two classes they name as having the most individual impact.\" Both counts are wrong against the source. Sclar et al. (lit/text/sclar2024.txt line 427: \"Table 1 shows that choices in S2, Fitem1, Fcasing do not independently predict performance differences\") name three of six constant classes as non-predictive, and the Table 1 rows I read (lines 470-479) confirm six classes: C 29%/1%, S1 43%/22%, S2 0%/0%, Fitem1 0%/0%, Fitem2 45%/10%, Fcasing 3%/0%. So three of six is HALF, not \"most\", and the complement is THREE (C, S1, Fitem2), not two -- S1 and Fitem2 are only the two singled out as having the greatest individual impact among those three. The paper's own \u00a77 (lines 3900-3913) states the corrected version (\"half\", \"Three classes carry a positive individual signal\", \"three classes of six\") and explicitly says the alternative framing is \"wrong against their table\", so the current build contradicts itself across \u00a72 and \u00a77. The builder's own comment at line 3890 documents the fix verbatim: '\"Most\" was wrong: they name three of six classes as non-predictive (S2, Fitem1, Fcasing), which is half. And the complement is three, not two -- C carries 29 % weak differences.' The correction was applied at \u00a77 only. Separately, \u00a72 renders the citation through TAB(\"instrument_validation\"), which is THIS paper's Table 1 (the instrument-validation table); it coincidentally reads \"Table 1\" and matches Sclar's number by accident -- exactly the hazard the \u00a77 comment says it hard-typed its reference to avoid.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, lines 1136-1141, replace the \u00a72 clause with the \u00a77 counts and hard-type the reference: change \"most individual \" / \"format features do not independently predict performance \u2014 in \" / \"their \" + TAB(\"instrument_validation\") + \", the second separator, the item wrapper and casing do \" / \"not \u2014 with two exceptions: separators and the number format \" / \"used in enumerations are the two classes they name as having the \" / \"most individual impact.\" to \"half the individual \" \"format features do not independently predict performance \u2014 in \" \"their Table 1, the second separator, the item wrapper and casing do \" \"not \u2014 while three classes carry a positive individual signal, two of \" \"which they name as having the most individual impact: separators and \" \"the number format used in enumerations.\" Leave the rest of the paragraph unchanged, then rebuild.",
      "evidence_command": "grep -n \"most individual format features\\|half the individual format features\\|two exceptions\\|three classes of six\\|\\\"Most\\\" was wrong\" \"paper-a/src/build_paper_v3.py\"; sed -n '427,431p;474,479p' \"lit/text/sclar2024.txt\""
    },
    {
      "id": "RW-5",
      "verdict": "CONFIRMED",
      "reason": "The clause is present verbatim in the current build (fresh PyMuPDF extract, \u00a75.2 p.17) and in build_paper_v3.py lines 3309-3310: \"with cache residency added, which neither account covers\". The claim is false as to He and Thinking Machines Lab. lit/text/he_2025_defeating_nondeterminism.txt line 297 names it: attention \"must deal with a variety of inference optimizations that affect how sequences get processed (chunked prefill, prefix caching, etc.)\". Line 303 states the residency case exactly: reduction order \"must be identical regardless of whether 0 tokens are in the KV cache (prefill) or 999 tokens are in the KV cache (decoding)\". Lines 305-308 are a dedicated subsection, \"FlashAttention with a KV Cache\", working the boundary-condition arithmetic and prescribing the fix (update the KV cache and page table before the attention kernel). That is the same mechanism the paper's Study 9 describes (\"llama.cpp reuses the cache for whatever prefix a new prompt shares with the previous one\"). Yuan et al. mention the KV cache only re: memory/quantization (lines 883, 915), so \"neither\" fails on He alone, which suffices. The repo's own lit note (lit/notes/he_2025_defeating_nondeterminism.md, \"Consequence for our paper\" point 2) rests the novelty claim on \"their analysis is about batch composition, not about which prefix happens to be resident\" \u2014 that premise is contradicted by source line 303. The overclaim sits in a paragraph that otherwise carefully cedes priority (\"The mechanism is not ours, and neither is the priority\"), so it reads as a lapse rather than a considered position. What is actually new is the quantification (93.1% to 100.0% agreement, per-cell SD 0.00000), not the coverage.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py lines 3309-3310, change the string \"with cache residency added, which neither account covers, and on a FAIRNESS \" to \"with cache residency separated as an independently manipulable knob and quantified, and on a FAIRNESS \". This deletes only the unsupported coverage claim and keeps the defensible contribution (He names prefix caching and prescribes a kernel-level fix, but does not isolate or quantify residency's standalone share). No other sentence needs to change; the lit note's point 2 rationale is also wrong but is not part of the built paper.",
      "evidence_command": "sed -n '297p;303p;305,308p' \"lit/text/he_2025_defeating_nondeterminism.txt\"; sed -n '3306,3311p' \"paper-a/src/build_paper_v3.py\"; C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz; d=fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf'); print([p.number+1 for p in d if 'neither account covers' in p.get_text()])\"",
      "severity": "major",
      "root_cause": "novelty-overclaim-vs-cited-source"
    },
    {
      "id": "RW-6",
      "verdict": "CONFIRMED",
      "reason": "The quoted sentence is verbatim in the current build (PDF p.23; builder `build_paper_v3.py:4073-4079`), and it still frames the panel as availability-based: \"it needs the whole matrix: every audit we could obtain in full text ... Table 18 is that matrix over 13 LLM hiring audits\". The artifact confirms the roster: `reporting_practice_matrix.json` has `n_studies=15`, `n_llm_hiring_audits=13`, and `llm_hiring_audits` = An 2024, Armstrong 2024, Fu & Shi 2025, Gaebler 2024, Gao 2026, Glazko 2024, Hoffstedde 2026, Iso 2025, Nghiem 2024, Seshadri 2025, Tan 2026, Veldanda 2023, Wilson & Caliskan 2024. Haim/Salinas/Nyarko appears nowhere: fresh PyMuPDF extraction of the current PDF has zero hits for \"Haim\" or \"Nyarko\", there is no PDF in `lit/` or text in `lit/text/`, and no screening/exclusion record anywhere in the repo (only the critique files themselves). I checked the paper rather than assuming: arXiv:2402.14875 (Salinas, Haim & Nyarko) defines five advice scenarios and the fifth is Hiring \u2014 \"Seeking advice during the process of making an initial job offer\", outcome = initial salary offer in dollars, across 42 prompt templates, with code and data public at github.com/AlexSalinas99/audit_llms. That is the same genre as Nghiem et al. 2024, which IS in the panel (its notes at `lit/notes/nghiem_et_al_2024.md` show its outcomes are employment recommendations and salary offers, not resume screens), so \"not a resume-screening audit\" does not distinguish it from a panel member. So an obtainable, hiring-relevant audit is missing from a set the paper says is everything it could obtain \u2014 and it is the worst possible one to omit silently, since the panel's headline rows are \"dispersion across wordings 0/8\" and \"code or data released 2/13\" and this study varies wording over 42 templates and released its code. The one point where the finding overreaches: whether Haim et al. belongs *in* Table 18 is a scope judgement (hiring is 1 of its 5 domains), and the counts do not change unless the author makes that call. What is not a judgement call is that the printed inclusion criterion is availability, and under availability the panel is not complete.",
      "minimal_fix": "Edit the single `P(...)` block at `paper-a/src/build_paper_v3.py:4073-4079`: replace the availability framing \"every audit we could obtain in full text\" with the topical criterion actually used (e.g. \"every LLM audit we could obtain in full text whose object of study is an employment decision\"), and append one clause naming the salient exclusion \u2014 that Haim, Salinas & Nyarko (2024, arXiv:2402.14875) is out of the panel because hiring is one of five advice domains there, noting that it varies wording across 42 templates. That keeps every count in Table 18 unchanged and needs no new full-text reading. Adding it to the panel instead would require ingesting the PDF into `lit/`, a two-reader pass under `_how_produced`, and would move the `code_or_data_released` and `dispersion_across_wordings` rows \u2014 an author decision, not a minimal fix.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz,json; d=fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf'); t=''.join(p.get_text() for p in d); print('claim_present=', 'could obtain in full text' in t, '| Haim=', 'Haim' in t, '| Nyarko=', 'Nyarko' in t); m=json.load(open(r'paper-a/data/reference/reporting_practice_matrix.json',encoding='utf-8')); print(m['n_llm_hiring_audits'], m['llm_hiring_audits'])\"  # prints: claim_present= True | Haim= False | Nyarko= False ; 13 [...]. Source line: paper-a/src/build_paper_v3.py:4074",
      "root_cause": "claim-outruns-evidence",
      "severity": "major"
    },
    {
      "id": "R2",
      "verdict": "CONFIRMED",
      "reason": "The current PDF (p.11, Table 10) still prints predicted shifts -0.0092/-0.0858/+0.0122/-0.0668 and obs/pred 2.17/1.07/2.20/0.47. analyze_length_prediction.py:122-127 computes predicted = -slope * mean(delta over pairs with delta != 0) while observed = cl[\"matched_minus_all\"][\"est\"] from name_length_effect.json, which is effect_token_matched - effect_all_pairs (e.g. llama-3.1: -0.24228 - (-0.15089) = -0.09138), i.e. matched vs ALL 48 -- the same contrast Table 9's \"difference\" column reports (+0.1699 - +0.1897 = -0.0198 for Llama-2-7B). Recomputing per-pair effects from data/names/names_*.jsonl and deltas from data/instrument/name_length_*.json reproduces the artifact exactly and shows the matched pairs are precisely those with delta = 0, so mean delta over all 48 = (n_dropped/48) x mean delta over dropped = 0.2500, 0.9167, 0.5000, 0.5000. Since y_matched - y_all48 = -b * d_all48, the prediction matching the observed contrast is -slope x d_all48 = -0.0063, -0.0643, +0.0081, -0.0445, giving obs/pred 3.15, 1.42, 3.30, 0.71. Every predicted-shift cell is inflated by 48/n_dropped (1.45, 1.33, 1.50, 1.50) and every obs/pred cell deflated by the same factor. The prose \"sitting at 1.07 on the model 4.4 discusses\" and \"ratio running 0.47 to 2.20\" are wrong for the same reason (they become 1.42 and 0.71 to 3.30). Sign agreement 4/4 and \"within a factor of five\" 4/4 are unaffected, so the section's conclusion survives, but the printed magnitudes and the headline 1.07 do not.",
      "minimal_fix": "In paper-a/src/analyze_length_prediction.py line 125, change `mean_d = float(np.mean([dl[k] for k in dropped]))` to `mean_d = float(np.mean([dl[k] for k in common]))` (keep `dropped` for the n_dropped count), rename the emitted field `mean_delta_over_dropped` -> `mean_delta_over_all` at line 132, update the `_prediction` string at lines 103-106 and the docstring formula at line 22 to \"-b * mean(delta over ALL 48 pairs)\", and rerun to regenerate data/instrument/length_prediction.json. Then in build_paper_v3.py: line 2229 \"over the pairs dropped\" -> \"over all 48 pairs\"; line 2240 field name; line 2245 column header \">mean \u0394, dropped\" -> \">mean \u0394, all 48\"; lines 2250-2252 caption \"over the pairs the matched subset drops\" -> \"over all 48 pairs\". The summary sentence at lines 2255-2262 reads from the artifact and updates itself (0.47-2.20 -> 0.71-3.30, 1.07 -> 1.42).",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;d=json.load(open(r'paper-a/data/instrument/length_prediction.json'))['models'];[print(m,'n_drop',v['n_dropped'],'pred_paper',round(v['predicted_shift'],4),'pred_all48',round(-v['slope']*v['mean_delta_over_dropped']*v['n_dropped']/48,4),'ratio_paper',round(v['ratio_observed_to_predicted'],2),'ratio_fixed',round(v['observed_shift']/(-v['slope']*v['mean_delta_over_dropped']*v['n_dropped']/48),2)) for m,v in d.items()]\"  # then see analyze_length_prediction.py:125 vs :127 and build_paper_v3.py:2229",
      "root_cause": "contrast-mismatch",
      "severity": "major"
    },
    {
      "id": "R3",
      "verdict": "CONFIRMED",
      "reason": "The current PDF (page 28, extracted fresh with PyMuPDF) still says \"the build fails rather than printing a number it cannot source\", emitted from build_paper_v3.py:4707-4708. The build does not fail. load() (line 126) appends the key to MISSING and returns None; the end of main() (lines 4825-4829) prints \"[WARNING] N artifact(s) missing; the corresponding sentences were dropped: ...\" and then \"return 0\". Only two paths abort: s2 is None (line 307, \"cannot build without study2_v2.json\") and the scale guard (line 644). So for 37 of the 38 entries in SRC the build exits 0 with a complete-looking PDF. The builder's own docstring (lines 14-17) states the true behavior verbatim and contradicts the paper: \"A sentence whose artifact is missing is DROPPED and a warning printed, so a partial run yields a shorter honest paper rather than a complete dishonest one.\" paper-a/src/audit_suppressed_prose.py exists for exactly this reason: \"That failure is invisible from the built PDF: a missing paragraph looks exactly like a paragraph that was never written.\" I checked the counter-hypothesis that the surrounding pipeline supplies the failure: release_version.py:76 gates on the build's return code (0), audit_consistency.main() always returns 0 (line 1003), and no test in research/tests/ references MISSING. corpus_size.json does exist, so MISSING is empty in the shipped run and no \"n/a\" appears in the PDF text \u2014 the warning path is simply never exercised, which is why the false claim survived.",
      "minimal_fix": "Edit the prose string at build_paper_v3.py:4707-4708 only; do not change the build's exit behavior, since drop-and-warn is the deliberate rule stated in the module docstring (lines 14-17). Replace \"and the build fails rather than printing a number it cannot source.\" with \"and a sentence whose artifact is missing is dropped with a warning rather than printed with a number the build cannot source.\" (Two adjacent string literals; the sentence is re-emitted on page 28 on the next build.)",
      "evidence_command": "sed -n '14,18p;4705,4708p;4824,4830p' \"paper-a/src/build_paper_v3.py\"",
      "severity": "major",
      "root_cause": "claim-outruns-implementation"
    },
    {
      "id": "R4",
      "verdict": "CONFIRMED",
      "reason": "The current build (figures/paper_instrument_validity_v3.pdf, byte-identical to releases/paper_instrument_validity_v12.pdf) still contains both passages, and they contradict each other. Section 2 (Background) reads \"most individual format features do not independently predict performance - in their Table 1, the second separator, the item wrapper and casing do not - with two exceptions: separators and the number format used in enumerations are the two classes they name as having the most individual impact.\" Section 7 (What the sensitivity is not) reads \"half the individual format features do not independently predict performance - in their Table 1 the second separator, the item wrapper and casing do not ... Three classes carry a positive individual signal, and they name two of those as having the most individual impact ... It establishes it for three classes of six and leaves three with a positive individual signal.\" Both list the SAME three non-predictive classes, so section 2's \"most\" is inconsistent with its own enumeration (three of six is half), and section 2's \"two exceptions\" contradicts section 7's \"three classes carry a positive individual signal.\" The author has already adjudicated this: build_paper_v3.py lines 3890-3894 state in a comment 'THE COUNTS, AGAINST SCLAR'S OWN TABLE 1 AND TEXT. \"Most\" was wrong: they name three of six classes as non-predictive (S2, Fitem1, Fcasing), which is half. And the complement is three, not two -- C carries 29 % weak differences; S1 and Fitem2 are the two they single out as having the MOST individual impact among those three.' Section 7's prose was rewritten to match that correction; the section 2 paragraph (build_paper_v3.py lines 1136-1141) was never updated. Adjacent latent bug in the same string: line 1138 renders \"their Table 1\" through TAB(\"instrument_validation\"), which is THIS paper's table number - the comment at lines 3896-3899 says section 7's reference was hard-typed precisely because the two numbers \"agree today only by coincidence\" and a reordering would silently renumber a reference to somebody else's table; section 2 still has the dynamic call. No repo artifact encodes Sclar's Table 1 (no \"sclar\" match in data/reference/published_effects.json), but the defect does not turn on that: the self-contradiction is present in the built PDF and the correct side is settled by the author's own comment.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, lines 1136-1141, replace the section 2 clause with wording matching section 7 and hard-type the foreign table reference: \"And, in their characterisation of the format space, half the individual format features do not independently predict performance - in their Table 1, the second separator, the item wrapper and casing do not - while three classes carry a positive individual signal, two of which they name as having the most individual impact: separators and the number format used in enumerations.\" Concretely: change \"most individual \" to \"half the individual \" (line 1136), replace the expression `TAB(\"instrument_validation\")` on line 1138 with the literal string \"Table 1\" (matching the deliberate hard-typing at line 3902 and the rationale at lines 3896-3899), and replace \"with two exceptions: separators and the number format used in enumerations are the two classes they name as having the most individual impact\" (lines 1139-1141) with \"while three classes carry a positive individual signal, two of which they name as having the most individual impact: separators and the number format used in enumerations\". Then rebuild.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz,re; t=re.sub(r'\\s+',' ',' '.join(p.get_text() for p in fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf'))); [print('...'+t[m.start()-160:m.start()+330]+'...\\n---') for m in re.finditer('individual format features', t)]\"  # then compare build_paper_v3.py:1136-1141 against build_paper_v3.py:3890-3913",
      "severity": "major",
      "root_cause": "correction-not-propagated"
    },
    {
      "id": "SC-03",
      "verdict": "CONFIRMED",
      "reason": "The 8.7 % bound is `_serving_bound`, computed at build_paper_v3.py:549-573 with an explicit `_m not in survives` skip on line 561. `survives` (line 317) is the two models whose Study-2 log-odds CI excludes zero: llama-2-7b-chat and llama-3.1-8b-instruct. But the serving manipulation ran on four checkpoints \u2014 rep_*.jsonl carries 180 cells at concurrency 4 and 180 at concurrency 1 for all four models, and cache_residual.json covers llama-2-7b-chat plus mistral-7b-instruct-v0.3. Recomputing max|effect_difference.ci| / |logodds.est| from the artifacts gives 0.087 and 0.056 on the two identified models, but 0.288 (mistral v0.1) and 0.162 (mistral v0.3) on the two excluded ones. So the current build's three claims \u2014 \u00a75.2 \"the shift is held within 8.7 % of the effect on every checkpoint we could manipulate\" (builder 3398-3401), \u00a79 item 7 \"bounded within 8.7 % of it on the checkpoints where we could manipulate them\" (4286-4288), and \u00a711 \"bounded within 8.7 % of the effect on every checkpoint where we could manipulate them\" (4663-4665) \u2014 all state a scope wider than the computation. The exclusion itself is statistically right (the builder comment at 3557-3560 says dividing by an effect covering zero has no finite upper bound), and the paper applies exactly that discipline elsewhere (Table 4 caption suppresses the point estimate on the two models whose \u03b2 covers zero; \u00a74.2 says \"On the two models whose race effect is identified\"); only these three sentences generalise. The overreach is made concrete by the clause immediately before the \u00a75.2 sentence, which quotes \"p = 0.54 and 0.85\" \u2014 llama-2-7b and mistral v0.3 \u2014 naming a manipulated checkpoint that the 8.7 % bound does not cover and whose ratio is 16.2 %.",
      "minimal_fix": "Replace the scope phrase at the three sites in paper-a/src/build_paper_v3.py so it names the identified checkpoints instead of all of them. Line 3399-3400: change `f\"held within {pct(_serving_bound, 1)} of the effect on every \" \"checkpoint we could manipulate \u2014 and it is the form in \"` to `f\"held within {pct(_serving_bound, 1)} of the effect on the two \" \"checkpoints whose effect is distinguishable from zero, the only \" \"ones a ratio against the effect is defined on \u2014 and it is the form in \"`. Line 4287: change `\"on the checkpoints where we could manipulate them, which is a bound \"` to `\"on the two checkpoints whose effect is distinguishable from zero, which is a bound \"`. Line 4664: change `\"effect on every checkpoint where we could manipulate them \u2014 a \"` to `\"effect on the two checkpoints whose effect is distinguishable from zero \u2014 a \"`. Do not widen the max at line 561 to all models; the identified-only restriction is correct and the builder comment at 3557-3560 explains why.",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json,pathlib; D=pathlib.Path(r'paper-a/data'); lo={m:v['overall']['logodds'] for m,v in json.loads((D/'delta_stability'/'study2_v2.json').read_text()).items()}; [print(f,m,'identified=%s'%(lo[m]['ci'][0]*lo[m]['ci'][1]>0),'ratio=%.3f'%(max(abs(x) for x in (v.get('effect_difference') or {}).get('ci'))/abs(lo[m]['est']))) for f in ('replicate_analysis.json','cache_residual.json') for m,v in json.loads((D/'replicate'/f).read_text()).items() if not m.startswith('_') and m in lo and (v.get('effect_difference') or {}).get('ci')]\"  # -> 0.087/0.056 on the two identified models, 0.288/0.162 on the two excluded ones; cf. build_paper_v3.py:561 and :3399",
      "severity": "major",
      "root_cause": "claim-outruns-evidence"
    },
    {
      "id": "SC-04",
      "verdict": "CONFIRMED",
      "reason": "Fresh PyMuPDF extraction of the current PDF (mtime Aug 2 17:32) page 3, \u00a71.1 bullet \"A complete account of the apparent nondeterminism,\" still reads \"Batching is manipulated on every checkpoint in the panel; cache residency on the two where a second session was run.\" \u00a73 \"Models\" explicitly names the six-checkpoint set as the panel: \"Six open-weight checkpoints... Llama-2-7B-chat, Llama-2-13B-chat, Llama-3.1-8B-Instruct, Mistral-7B-Instruct v0.1 and v0.3, and Mistral-7B v0.1 base. The panel spans a generational boundary.\" But batching (concurrency) was manipulated on only four. data/replicate/replicate_analysis.json has exactly four non-underscore keys: llama-2-7b-chat, llama-3.1-8b-instruct, mistral-7b-instruct-v0.1, mistral-7b-instruct-v0.3. Recomputing from raw JSONL rather than the summary: scanning every *.jsonl under data/ for a concurrency field, only the four rep_*.jsonl files vary it (180 records at concurrency:4 and 180 at concurrency:1 each); nothing in the tree varies concurrency on Llama-2-13B-chat or Mistral-7B v0.1 base, and no rep_llama-2-13b-chat.jsonl or rep_mistral-7b-v0.1-base.jsonl exists. \u00a75.2 of the same paper says four throughout (\"three of the four models\", \"positive on all 4 models\") and uses the correctly hedged \"every checkpoint we could manipulate\"; \u00a71.1 drops that scope. The cache-residency half is accurate (cache_residual.json has two models, both with _cross_session entries matching nocache_session2_*.jsonl). The error lands in the one sentence whose job is to disclose the coverage limits of the word \"complete\", which is why it is major rather than minor.",
      "minimal_fix": "In paper-a\\src\\build_paper_v3.py lines 1041-1042, replace \"Batching is manipulated on every \" / \"checkpoint in the panel;\" with \"Batching is manipulated on four of \" / \"the six checkpoints;\" so the bullet reads \"...Batching is manipulated on four of the six checkpoints; cache residency on the two where a second session was run, which is where the word \"complete\" is doing more work than the design supports.\" No other text or artifact changes needed; the cache-residency clause is already correct.",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json;print([k for k in json.load(open(r'paper-a/data/replicate/replicate_analysis.json',encoding='utf-8')) if not k.startswith('_')])\"  # -> 4 models; compare build_paper_v3.py:1041 (\"every checkpoint in the panel\") against build_paper_v3.py \u00a73 Models (\"Six open-weight checkpoints... The panel\")",
      "root_cause": "claim-outruns-evidence",
      "severity": "major"
    },
    {
      "id": "SC-05",
      "verdict": "CONFIRMED",
      "reason": "The current build still says, twice, that the business-analyst posting is \"the occupation where the effect is largest and the dispersion smallest for two of the four models\" (build_paper_v3.py:2527-2529 in \u00a74.5, and :4437-4439 in \u00a76). The conjunction holds on exactly one model. From data/occupation/occupation_analysis.json (fields ps and ps_sd_across_wordings, cross-checked against occupation_dispersion_null.json sd_by_posting and re-derived from occ_*.jsonl, which reproduces the JSON to 10 decimals): llama-2-7b-chat BA |ps-0.5|=.2894 vs SWE .0602 / RN .0694 (largest) and sd .0966 vs .1307 / .1262 (smallest) -> both hold. llama-3.1-8b-instruct BA effect .3125 is largest, but BA sd .0778 is NOT smallest -- SWE is .0574 -> conjunction fails. mistral-v0.1 BA effect .0579 is smallest of three (RN .1782 largest) and BA sd .0685 largest -> fails. mistral-v0.3 BA effect .0694 (RN .1111 largest) -> fails, and BA sd .0383805155 exactly ties SWE. So BA is largest-effect on two models and strictly smallest-dispersion on one, and those are not the same two models. The error survives reparsing (reading \"for two of the four models\" as attaching only to the dispersion clause makes the effect clause an unqualified claim false on two of four) and survives a scale change (on log-odds sigma_variant_raw the conjunction holds on zero models: llama-2 BA .0671 > RN .0615).",
      "minimal_fix": "De-conjoin the two counts in both hardcoded strings, keeping the plural referent that the next sentence (\"the same models\") depends on. build_paper_v3.py:2527-2529: change \"the occupation where the effect is largest and the dispersion smallest for two of the four models\" to \"the occupation where the effect is largest for two of the four models, and the dispersion smallest for one of them\". build_paper_v3.py:4438-4439: change \"where the effect is largest and the dispersion smallest for two of four models\" to \"where the effect is largest for two of four models and the dispersion smallest for one of them\". No artifact field changes; both sentences are literal prose in the builder, not computed.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;d=json.load(open(r'paper-a/data/occupation/occupation_analysis.json'));[print(m,'eff_argmax=',max(('BA','SWE','RN'),key=lambda k:abs(d[m][k]['ps']-0.5)),'sd_argmin=',min(('BA','SWE','RN'),key=lambda k:d[m][k]['ps_sd_across_wordings']),{k:(round(abs(d[m][k]['ps']-0.5),4),round(d[m][k]['ps_sd_across_wordings'],4)) for k in ('BA','SWE','RN')}) for m in d]\"  # then: grep -n \"dispersion smallest\" \"paper-a/src/build_paper_v3.py\"",
      "severity": "major",
      "root_cause": "claim-outruns-evidence"
    },
    {
      "id": "table6-caption-posterior-medians",
      "verdict": "CONFIRMED",
      "reason": "The current PDF caption for Table 6 still reads \"The first three columns are ratios of posterior MEDIANS.\" (build_paper_v3.py:1811-1812). Only the denominator is a posterior median: sd_from_wording_choice = 0.06341742724180222 is variance_components.json[m][\"all\"][\"sigma_variant\"][1], and index 1 is np.percentile(a, 50) per fit_variance_components.py:177-180. The numerator sd_from_name_draw is c[\"beta_sd\"] (fit_name_variance.py:170), which fit_name_variance.py:451 defines as e.std(ddof=1) over n_boot=4000 cluster-bootstrap resamples of the 12 matched first-name pairs (counterfactual_draws, lines 411-455) \u2014 a frequentist Monte-Carlo SD with no posterior anywhere in its derivation. I reproduced it exactly from the raw JSONL: rng=default_rng(20260729) over names_llama-2-7b-chat.jsonl (12 pairs x 144 obs) gives 0.07664740216971208, bit-identical to the artifact field, and 0.076647/0.063417 = 1.2086 -> the printed 1.21x. Every one of the twelve printed cells matches this bootstrap-over-posterior-median mix; the pure posterior-median ratio sigma_first[1]*sqrt(2)/(sqrt(k)*sigma_variant[1]) would instead give 1.18/0.84/0.68, 1.53/1.08/0.89, 3.57/2.52/2.06, 3.65/2.58/2.11 \u2014 different from the printed 1.21/0.85/0.68, 1.42/1.00/0.81, 3.24/2.32/1.87, 3.61/2.54/2.05. Related but outside the minimal fix: the fourth column IS the pure posterior construction (analyze_asymmetry_uncertainty.py, ratio(k) = sigma_first*sqrt(2)/(sqrt(k)*sigma_variant)), so the bound rests on a different numerator than the three point estimates beside it (Mistral v0.1 k=9 prints 1.87x next to [0.72, 8.35] whose own centre is 2.06), and that script's docstring line 7 repeats the same misdescription. No other passage in the current build corrects the caption.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, replace the sentence on lines 1811-1812 \u2014 currently `\"The first three columns are ratios of posterior \"` / `\"MEDIANS. The last propagates both posteriors and is a \"` \u2014 with `\"The first three columns divide a BOOTSTRAP standard \"` / `\"deviation over 4,000 resampled name lists by the posterior \"` / `\"median of the between-wording standard deviation. The last \"` / `\"propagates both posteriors and is a \"`, leaving the rest of the caption unchanged, then rebuild. (Optionally also correct the same claim in analyze_asymmetry_uncertainty.py:7, which is a docstring only.)",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz,json,numpy as np,re; from collections import defaultdict; t=chr(10).join(p.get_text() for p in fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf')); i=t.find('The first three columns'); print(repr(t[i:i+110])); nv=json.load(open(r'paper-a/data/names/name_variance.json',encoding='utf-8'))['llama-2-7b-chat']; print('artifact sd_from_name_draw', nv['averaging_asymmetry_with_replacement']['draw_3_per_race']['sd_from_name_draw'], 'ratio', nv['averaging_asymmetry_with_replacement']['draw_3_per_race']['ratio_name_to_wording']); rows=[json.loads(l) for l in open(r'paper-a/data/names/names_llama-2-7b-chat.jsonl',encoding='utf-8') if l.strip()]; by=defaultdict(list); [by[(r['white_first'],r['black_first'])].append(r['white_margin']-r['black_margin']) for r in rows]; pr=sorted(by); rng=np.random.default_rng(20260729); e=np.array([np.mean([v for i in rng.choice(len(pr),size=3,replace=True) for v in by[pr[i]]]) for _ in range(4000)]); print('recomputed bootstrap SD', e.std(ddof=1))\" ; # also: build_paper_v3.py:1811-1812 and fit_name_variance.py:170,451",
      "severity": "major",
      "root_cause": "caption-misdescribes-estimator"
    },
    {
      "id": "STAT-2",
      "verdict": "CONFIRMED",
      "reason": "The current build (fresh PyMuPDF extract) still prints the universal form at three sites: \u00a75.2 \"the shift is held within 8.7 % of the effect on every checkpoint we could manipulate\", \u00a79 item 7 \"bounded within 8.7 % of it on the checkpoints where we could manipulate them\", and the Conclusion \"bounded within 8.7 % of the effect on every checkpoint where we could manipulate them\" (build_paper_v3.py:3399-3400, 4286-4287, 4663-4664). But _serving_bound (build_paper_v3.py:549-573) skips any model `not in survives` at line 561, and survives (line 317) is exactly the two models whose Study-2 log-odds CI excludes zero: llama-2-7b-chat and llama-3.1-8b-instruct. Recomputing max|effect_difference.ci|/|logodds.est| from data/replicate/replicate_analysis.json and data/replicate/cache_residual.json against data/delta_stability/study2_v2.json gives: llama-2-7b conc 8.7 % (the quoted figure), llama-3.1-8b conc 5.6 %, mistral-v0.1 conc 28.8 %, mistral-v0.3 conc 16.2 %, llama-2-7b cache 8.0 %, mistral-v0.3 cache 4.1 %. Concurrency was manipulated on all four checkpoints and cache on two, so 8.7 % holds on 3 of the 6 manipulated cells, not \"every\" one; the true panel-wide bound is 28.8 %. The mismatch is visible inside one sentence: the p-values printed alongside it (\"p = 0.54 and 0.85\") come from `diffs` (line 3358 over `cm`, all cache models including the unidentified Mistral v0.3), so the p-values span the full manipulated panel while the ratio silently spans the identified subset. The paper states the opposite convention explicitly elsewhere \u2014 \u00a79 item 1 \"on the models where the effect is identified. Where it is not identified the ratio is larger still\", and the Table 4 caption suppresses the ratio \"on the two models whose \u03b2 interval covers zero, because a ratio against such a denominator has no finite upper bound\" \u2014 so the restriction is principled but undisclosed at these three sites. Defect is labelling/quantifier scope, not arithmetic.",
      "minimal_fix": "Replace the universal quantifier with the paper's own existing qualifier at the three prose sites in paper-a/src/build_paper_v3.py, leaving _serving_bound and its computation untouched: line 3399-3400 \"of the effect on every checkpoint we could manipulate\" -> \"of the effect on every checkpoint whose effect is identified\"; line 4287 \"on the checkpoints where we could manipulate them\" -> \"on the checkpoints whose effect is identified\"; line 4663-4664 \"of the effect on every checkpoint where we could manipulate them\" -> \"of the effect on every checkpoint whose effect is identified\". If the universal quantifier over the whole manipulated panel is wanted instead, keep the wording and switch to the absolute bound: drop the `/ abs(lo[_m][\"est\"])` division and the `_m not in survives` guard at build_paper_v3.py:561, giving |shift| <= 0.016 log-odds on all six manipulated cells.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;s2=json.load(open('data/delta_stability/study2_v2.json'));lo={m:v['overall']['logodds'] for m,v in s2.items() if not m.startswith('_')};[print('%-24s %-10s ratio=%5.1f%% identified=%s'%(m,f[15:24],100*max(abs(x) for x in v['effect_difference']['ci'])/abs(lo[m]['est']),lo[m]['ci'][0]*lo[m]['ci'][1]>0)) for f in ['data/replicate/replicate_analysis.json','data/replicate/cache_residual.json'] for m,v in json.load(open(f)).items() if not m.startswith('_') and 'effect_difference' in v]\"  # run from paper-a ; compare with build_paper_v3.py:317, 561, 3399, 4287, 4663",
      "severity": "major",
      "root_cause": "claim-outruns-evidence"
    },
    {
      "id": "STAT-3",
      "verdict": "CONFIRMED",
      "reason": "The current PDF prints two different 95 % intervals for the same estimand with identical point estimates. \u00a74.1 (p. 6): \"the dispersion-to-effect ratios the argument rests on become Llama-2-7B-chat, 0.33 [0.19, 0.42]; Llama-3.1-8B-Instruct, 0.25 [0.14, 0.32]\". \u00a74.7: \"Llama-2-7B-chat 0.33 [0.24, 0.66]; Llama-3.1-8B-Instruct 0.25 [0.18, 0.50] locally\". The point estimates are byte-identical across artifacts (0.3337064578338273 and 0.2489033184850334 appear in both dispersion_uncertainty.json ratio and ratio_intervals.json local est/published_ratio), so it is the same quantity. The mechanism is as reported: analyze_dispersion_uncertainty.py:87 computes ratios[b] = boots[b] / eff with eff a fixed scalar, and dividing the stored sd_ci by effect_ps reproduces \u00a74.1's bounds exactly (0.0553447/0.2893519 = 0.19127; 0.1220183/0.2893519 = 0.42170), confirming the denominator is held at a point estimate; ratio_intervals.json _method states it resamples name pairs and recomputes numerator and denominator per draw. \u00a74.7's own prose states the rule \u00a74.1's number violates: \"numerator and denominator move together, so they have to be resampled together\". A full-text search of the fresh extract found no sentence anywhere relating the two intervals, and neither section cross-references the other. Two corrections to the finding as written: the intervals are NOT mutually incompatible \u2014 they overlap ([0.24, 0.42] and [0.18, 0.32]) \u2014 and \u00a74.1 does say \"Resampling wordings\", so its unit is not unlabelled. What remains is a genuine unreconciled inconsistency on the paper's headline statistic. No substantive \u00a74.1 claim flips under the wider interval (its lower bounds only rise, and \"pin the dispersion to about a factor of two\" refers to the SD interval, which is unaffected), so this is a reconciliation defect, not a numerical error.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, lines 1503-1507, change the \u00a74.1 ratio interval to read from the name-pair bootstrap already on disk instead of the fixed-denominator one: replace the generator's `_du[m]['ratio_ci'][0]` / `_du[m]['ratio_ci'][1]` with `rint['local'][m]['ci'][0]` / `rint['local'][m]['ci'][1]` (rint is already loaded at line 286), so \u00a74.1 prints 0.33 [0.24, 0.66]; 0.25 [0.18, 0.50] \u2014 identical to \u00a74.7. Leave line 1500's \"Resampling wordings\" attached to the SD interval only, which stays correct, and adjust the connective at line 1503 to \"and, resampling name pairs so that numerator and denominator move together (\u00a74.7), the dispersion-to-effect ratios the argument rests on become\". This is one expression change plus one clause; no artifact needs regenerating, since data/reference/ratio_intervals.json already contains both local intervals.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json; a=json.load(open(r'paper-a/data/delta_stability/dispersion_uncertainty.json')); b=json.load(open(r'paper-a/data/reference/ratio_intervals.json')); [print(m,'pt',a['models'][m]['ratio'],'sec4.1_ci',a['models'][m]['ratio_ci'],'sec4.7_ci',b['local'][m]['ci']) for m in ('llama-2-7b-chat','llama-3.1-8b-instruct')]\"  # then compare build_paper_v3.py:1503-1507 (uses _du[m]['ratio_ci']) against build_paper_v3.py:2891-2903 (uses rint local ci), and analyze_dispersion_uncertainty.py:87 (ratios[b] = boots[b] / eff, eff fixed)",
      "root_cause": "two-intervals-one-estimand",
      "severity": "major"
    },
    {
      "id": "STAT-6",
      "verdict": "CONFIRMED",
      "reason": "The current build (fresh PyMuPDF extract, \u00a74.5) reads: \"The business-analyst posting \u2014 the one every other study in this paper is built on \u2014 happens to be the occupation where the effect is largest and the dispersion smallest for two of the four models. Had we chosen the software-engineer posting as the primary stimulus, the same models would have looked as though they had no measurable effect at all...\" The same claim is repeated in the limitations section: \"\u00a74.5 reports that the business-analyst posting is where the effect is largest and the dispersion smallest for two of four models\". Both strings are hardcoded (build_paper_v3.py:2528 and :4438), not computed. I recomputed the underlying quantities from the raw JSONL (BA from data/delta_stability/delta_*.jsonl, SWE/RN from data/occupation/occ_*.jsonl; 432 pairs \u00d7 12 wordings per cell) and reproduced occupation_analysis.json exactly. Effect |Ps\u22120.5| argmax by posting: llama-2 BA (0.289 vs 0.060, 0.069), llama-3.1 BA (0.313 vs 0.095, 0.220), mistral-v0.1 RN, mistral-v0.3 RN. Between-wording SD (ps_sd_across_wordings, the quantity the paper calls \"dispersion\" and the one occupation_dispersion_null.json permutes) argmin: llama-2 BA (0.0966 vs 0.1307, 0.1262), llama-3.1 SWE (0.0574 < BA 0.0778), mistral-v0.1 SWE (BA is in fact the largest, 0.0685), mistral-v0.3 BA \u2014 but only by 1e-17 over SWE (0.038380515539319850 vs 0.038380515539319860, an exact tie broken by float noise). So BA is the max-effect posting on {llama-2, llama-3.1} and the min-dispersion posting on {llama-2, mistral-v0.3 (tied)}. The conjunction holds on Llama-2-7B-chat alone \u2014 one model, not two \u2014 exactly as the finding states, including its parenthetical about the Mistral tie. The following sentence's \"the same models\" presupposes one identified pair and so forces the conjunctive reading; under the distributive reading (\"largest for two \u2026 smallest for two\") there is no single set of \"the same models\". The only rescue is reading \"dispersion smallest\" as \"dispersion-to-EFFECT smallest\", under which BA wins on {llama-2, llama-3.1} and the count is right; but \u00a77 itself maps that half onto the bare-SD ranking (\"\u00a74.5 finds the between-posting differences in DISPERSION indistinguishable \u2026 so the ranking of postings by dispersion is a point estimate\"), and \u00a74.5 has just withdrawn cross-posting ratio comparisons because 8 of 12 denominators cover zero \u2014 including both non-BA cells on llama-2. Severity is minor rather than major: the misstatement runs against the authors (it overstates how favourable their own draw was), and each half is separately true at 2 of 4.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py line 2528, replace \"where the effect is largest and the dispersion smallest for two of \" / \"the four models.\" with \"where the effect is largest on two of the four models and where the dispersion is smallest on two \u2014 the same model on both counts only for Llama-2-7B-chat, and on Mistral v0.3 the dispersion tie is exact.\" Apply the parallel edit at line 4438 (\"is where the effect is largest and the dispersion smallest for \" / \"two of four models\"). If \"the same models\" at line 2530 is meant to name the max-effect pair (Llama-2-7B-chat and Llama-3.1-8B-Instruct, the two whose SWE effect interval covers 0.5), say so explicitly rather than leaving the referent to the withdrawn conjunction.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;d=json.load(open(r'paper-a/data/occupation/occupation_analysis.json'));O=['BA','SWE','RN'];[print(m,'effect_argmax=',max(O,key=lambda o:abs(d[m][o]['ps']-0.5)),'| sd_argmin=',min(O,key=lambda o:d[m][o]['ps_sd_across_wordings']),'| sds=',[round(d[m][o]['ps_sd_across_wordings'],6) for o in O]) for m in d]\"  # \u2192 BA/BA, BA/SWE, RN/SWE, RN/BA; claim text at paper-a/src/build_paper_v3.py:2528 and :4438",
      "severity": "minor",
      "root_cause": "conjunction-over-different-model-subsets"
    },
    {
      "id": "RW-2",
      "verdict": "CONFIRMED",
      "reason": "The sentence is present verbatim in the current build (fresh PyMuPDF extraction, \u00a78.1 p.23; source build_paper_v3.py:4080-4081): \"Each cell was filled by reading the paper end to end, and every NEGATIVE was re-checked independently against a fresh extraction of the PDF\". The released artifact does not support \"every\". In paper-a/data/reference/reporting_practice_matrix.json, the negative_check field \u2014 which build_reporting_matrix.py's docstring defines as the blind second-reader re-extraction \u2014 is a non-empty string on exactly 6 of the 13 llm_hiring_audit studies (An, Fu & Shi, Gao, Iso, Tan, Wilson & Caliskan, all source_file=reporting_matrix_agents_round1.json) and null on the other 7 (Armstrong, Gaebler, Glazko, Hoffstedde, Nghiem, Seshadri, Veldanda, all source_file=reporting_matrix_agents_round2.json). I checked the raw file directly: reporting_matrix_agents_round2.json rows carry only {study, included, local_pdf, citation_check, fields}, no negative_check key on any of its 8 rows, and zero occurrences of \"re-check\", \"second reader\", \"fresh extraction\", \"PyMuPDF\", or \"re-extract\"; its per-cell evidence is single-reader grep output. load_rounds() (build_reporting_matrix.py:199) can only populate the field from that key, so the absence is structural. Recounting the cells: 94 not-reported verdicts across the 13 audits, 47 in studies with a re-check and 47 without; restricted to Table 18's 11 printed rows, 72 negatives with 37 unbacked. The four \"0 / N\" rows are the worst: concurrency_or_batching 7 of 11 negatives unbacked, cache_policy 6 of 11, token_matching 6 of 11, dispersion_across_wordings 2 of 4. No caveat appears elsewhere in the PDF (\"re-check\" occurs once in \u00a78.1; \"second reader\" zero times). The claim is mirrored in two more places that are equally unsupported: the _how_produced field of reporting_practice_matrix.json (emitted at build_reporting_matrix.py:303) and the generated-note boilerplate at build_lit_notes.py:109 \u2014 which is stamped onto exactly the six notes where build_lit_notes.py:141 suppresses the \"## Independent re-check of every negative\" section for want of data (verified: grep -c \"Independent re-check\" returns 0 for armstrong/gaebler/glazko/hoffstedde/nghiem/veldanda notes).",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, replace lines 4080-4081 (\"the artifacts. Each cell was filled by reading the paper end to end, \" / \"and every NEGATIVE was re-checked independently against a fresh \") with: \"the artifacts. Each cell was filled by reading the paper end to end, \" / \"and for six of the thirteen audits every NEGATIVE was additionally \" / \"re-checked by a second reader working from a fresh \" \u2014 leaving line 4082 (\"extraction of the PDF \u2014 necessary, because several of these \") unchanged, so the sentence reads \"\u2026re-checked by a second reader working from a fresh extraction of the PDF \u2014 necessary, because\u2026\". For consistency the same scope qualifier should be applied to the mirrored strings at build_reporting_matrix.py:303 (the _how_produced artifact field) and build_lit_notes.py:109 (the generated-note boilerplate), then both scripts and the paper re-run.",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json;d=json.load(open(r'paper-a/data/reference/reporting_practice_matrix.json',encoding='utf-8'));a=[s for s in d['studies'] if s['kind']=='llm_hiring_audit'];print(len(a),'audits');print('WITH negative_check:',[s['label'] for s in a if s.get('negative_check')]);print('WITHOUT:',[s['label'] for s in a if not s.get('negative_check')]);print('not-reported cells backed/unbacked:',sum(sum(1 for c in s['cells'].values() if c['verdict']=='not-reported') for s in a if s.get('negative_check')),'/',sum(sum(1 for c in s['cells'].values() if c['verdict']=='not-reported') for s in a if not s.get('negative_check')))\"  # then see build_paper_v3.py:4080-4081",
      "severity": "major",
      "root_cause": "claim-outruns-evidence"
    },
    {
      "id": "A5",
      "verdict": "CONFIRMED",
      "severity": "major",
      "root_cause": "abstract-contradicts-body",
      "reason": "Present in the current build (fresh PyMuPDF extract, 29pp). The abstract still reads \"...by 61 % [7 %, 157 %] on the largest, a shift not itself separable from zero on the name-pair resampling unit\" (build_paper_v3.py:719-726), and the identical hardcoded clause appears twice more in \u00a71.2 (lines 926-927, 1014-1015); \u00a710.1 restates it as \"With the correct resampling unit that difference is not distinguishable from zero in any case\" (lines 4410-4412). The artifact data/instrument/name_length_effect.json -> llama-3.1-8b-instruct -> token_matched_first_name_clustered records three verdicts on the SAME name-pair (first_name_pair) unit, not one: matched_minus_all.ci = [-0.21877, -0.00932] (excludes zero) with difference_significant_by_ci = true; matched_minus_all.p = 0.07805 with difference_significant = false; cluster_relabel_test.p = 0.0454545 (exact = true, n_outcomes = 220 = C(12,3)) with difference_significant_by_relabel = true. The abstract asserts the single flag difference_significant = false, i.e. the bootstrap p, which \u00a74.4 of this same build explicitly disavows: \"A bootstrap with that much atom in it supports neither statistic well\" and \"That is the number this section rests on: not the bootstrap, which is degenerate on three clusters.\" \u00a74.4 then refuses the verdict outright: \"Three procedures, three answers straddling 0.05. The reportable finding is that the token-matched subset is too small to adjudicate the question it was introduced to settle \u2014 not that the effect grows by 61 %, and not that it does not.\" Two independent confirmations that the abstract is wrong on the facts, not merely selective: (1) the \"[7 %, 157 %]\" it prints is growth_ratio.ci = [0.06599, 1.56643], and analyze_name_length.py:271-284 builds `rat` inside the same cluster (name-pair) bootstrap loop as `dif`, so that is a name-pair-unit 95 % interval whose lower bound is +7 % \u2014 an interval excluding zero cannot be described as \"not separable from zero\" in its own sentence; (2) the exact cluster-relabelling test, which is the paper's stated basis and does not depend on the bootstrap, gives p = 0.045 < 0.05 on that unit. So on the name-pair resampling unit two of three procedures separate the shift from zero, and the abstract reports the third as settled fact.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, replace the hardcoded clause \"a shift not itself separable from zero on the name-pair resampling unit\" with \"a subset too small to settle the size of that shift: three procedures on the name-pair unit straddle 0.05 (\u00a74.4)\". Apply at line 724-725 (abstract), line 926-927 (\u00a71.2 dispersion budget; adjust to \"though the token-matching shift is not settled in size once the resampling unit is the name pair (\u00a74.4)\"), and line 1014-1015 (\u00a71.2 contributions). Also fix \u00a710.1 at lines 4410-4412: drop \"that difference is not distinguishable from zero in any case\" in favour of \"that difference is not firmly established in any case\". No artifact field changes; no re-analysis needed \u2014 the three flags difference_significant / difference_significant_by_ci / difference_significant_by_relabel already exist in the JSON and the builder is simply asserting the first of them.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;d=json.load(open(r'paper-a/data/instrument/name_length_effect.json',encoding='utf-8'))['llama-3.1-8b-instruct']['token_matched_first_name_clustered'];print('diff ci',d['matched_minus_all']['ci']);print('boot p',d['matched_minus_all']['p']);print('exact relabel p',d['cluster_relabel_test']['p'],d['cluster_relabel_test']['exact'],d['cluster_relabel_test']['n_outcomes']);print('ratio ci',d['growth_ratio']['ci']);print('flags',d['difference_significant'],d['difference_significant_by_ci'],d['difference_significant_by_relabel'])\"  # then see build_paper_v3.py:724 vs the PDF \u00a74.4 sentence \"Three procedures, three answers straddling 0.05.\""
    },
    {
      "id": "A6",
      "verdict": "CONFIRMED",
      "reason": "The current PDF (fresh PyMuPDF extract, Table 1 caption on p.5) still reads \"\u2026the top-100 window captures at all; the two are divided in the next column.\" But build_paper_v3.py:1251 computes the \"share of top-100\" column as sh = mean over probes of (addressed_mass / top_total_mass), while columns 4 and 5 are the separate means of addressed_mass and top_total_mass (lines 1246-1250). Mean-of-ratios is not the ratio-of-means, and the gap is visible in the printed digits for 3 of 6 rows: Llama-2-7B prints 8.24 % / 99.2 % / 8.33 % but 8.24/99.2 = 8.31 % (recomputed from the 48 records in data/instrument/token_coverage_llama-2-7b-chat.json: mean-of-ratios 8.3311 %, ratio-of-means 8.3062 %; per-probe ratios span 0.019-0.173, so this is a genuine Jensen gap, not a rounding artifact). Llama-2-13B prints 14.60 % vs quotient 14.61 %, and Mistral base prints 9.72 % vs quotient 9.73 %. The other three rows happen to agree. No caveat about mean-of-ratios appears anywhere in the PDF text.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, in the Table 1 caption string, replace the fragment on lines 1288-1289 \u2014 \"distribution the top-100 window captures at all; the two \" / \"are divided in the next column. \" \u2014 with \"distribution the top-100 window captures at all; the next \" / \"column is the mean over probes of their per-probe ratio, which \" / \"is not in general the quotient of the two column means. \" Then rebuild. No artifact or numeric change is needed; only the caption's arithmetic claim is wrong.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json,numpy as np; v=json.load(open(r'paper-a/data/instrument/token_coverage_llama-2-7b-chat.json',encoding='utf-8')); print('mean-of-ratios %.4f'%(np.mean([r['addressed_mass']/r['top_total_mass'] for r in v])*100), 'ratio-of-means %.4f'%(np.mean([r['addressed_mass'] for r in v])/np.mean([r['top_total_mass'] for r in v])*100))\"  # prints 8.3311 vs 8.3062; see build_paper_v3.py:1251 (sh) and build_paper_v3.py:1288-1289 (caption)",
      "severity": "minor",
      "root_cause": "caption-describes-wrong-statistic"
    },
    {
      "id": "A8",
      "verdict": "CONFIRMED",
      "severity": "minor",
      "root_cause": "count-scope-mismatch",
      "reason": "The current build (fresh PyMuPDF extract of paper_instrument_validity_v3.pdf, \u00a73) reads: \"64,397 model calls yielding 31,468 matched-pair records across eleven studies, plus 1,461 single-prompt records from the admission gate, the affiliation probe and the instrument smoke tests.\" The finding's proposed \"plus 1,461 single-prompt records\" clause is ALREADY present, so only the \"eleven\" is still wrong \u2014 but it is wrong. data/reference/corpus_size.json publishes n_studies=11 AND n_studies_matched_pair=9, and its by_study map shows panel_gate {n_matched_pair_records: 0, n_single_prompt_records: 1080}, prestige {0, 192}, smoke {0, 189}. I recomputed from the raw JSONL with the builder's own predicate (both arms measured, via white/black_margin or white/black_raw, quarantine dirs excluded) and got pair=31468, single=1461, calls=64397, with matched pairs coming from exactly 9 directories (delta_stability, frontier, mechanism, mechanism_panel, names, occupation, quantization, replicate, second_task). Since n_studies=11 excludes only smoke, panel_gate and prestige are inside the \"eleven\" \u2014 so the sentence credits all 31,468 pairs to eleven studies while in the same breath naming two of those eleven as sources of single-prompt records only. build_paper_v3.py:437 binds _n_studies to _corpus[\"n_studies\"] (11); line 1223 uses it for the matched-pair clause, and that line's own hardcoded fallback string is \"nine\", confirming the intended value. The other two _n_studies sites (lines 983 and 4425) describe the whole enterprise, not the paired corpus, and are correct at eleven.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, make ONLY line 1223 read the matched-pair study count instead of the total. Add next to line 437: `_n_studies_mp = _corpus[\"n_studies_matched_pair\"] if _corpus else None`, then change line 1223 from `+ (NUM.get(_n_studies, str(_n_studies)) if _n_studies else \"nine\")` to `+ (NUM.get(_n_studies_mp, str(_n_studies_mp)) if _n_studies_mp else \"nine\")` and line 1224 from `+ \" studies, plus \"` to `+ \" matched-pair studies, plus \"`. This renders \"31,468 matched-pair records across nine matched-pair studies, plus 1,461 single-prompt records ...\". Leave lines 983 and 4425 on _n_studies (eleven) \u2014 they are correct. The artifact needs no change; n_studies_matched_pair=9 already exists. Note the stale `_n_studies_note` in corpus_size.json still says \"There are 10 data directories but the paper says nine studies ... Wire prose that says 'nine studies' to n_studies\", which is what caused the miswiring; n_data_directories is now 12 and n_studies is 11, so that note should be corrected to point matched-pair prose at n_studies_matched_pair.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;c=json.load(open(r'paper-a/data/reference/corpus_size.json',encoding='utf-8'));print('n_studies',c['n_studies'],'n_studies_matched_pair',c['n_studies_matched_pair']);print('dirs with pairs:',[k for k,v in c['by_study'].items() if v['n_matched_pair_records']>0]);print('single-only:',{k:v['n_single_prompt_records'] for k,v in c['by_study'].items() if v['n_matched_pair_records']==0})\"   # then see build_paper_v3.py:437 and :1223"
    },
    {
      "id": "STAT-1",
      "verdict": "CONFIRMED",
      "reason": "The sentence is still in the current PDF at \u00a74.1 (p. 6, section spans offsets 31333-39436 of freshly extracted text): \"the dispersion-to-effect ratios the argument rests on become Llama-2-7B-chat, 0.33 [0.19, 0.42]; Llama-3.1-8B-Instruct, 0.25 [0.14, 0.32]\". Those intervals come from build_paper_v3.py:1504-1507 reading dispersion_uncertainty.json -> models[m].ratio_ci, which analyze_dispersion_uncertainty.py builds as ratios[b] = boots[b] / eff (line 86) with eff fixed once at line 79 outside the bootstrap loop. Proof it carries no denominator uncertainty: the ratio CI's relative width equals the SD CI's relative width to full precision (llama-2-7b-chat: sd_ci_relative_width = 0.6904988796948374; (0.42169527990114974 - 0.1912713446199595)/0.3337064578338273 = 0.690498879694837) - the ratio interval is a pure rescaling of the SD interval. Meanwhile data/reference/ratio_intervals.json holds a name-pair cluster bootstrap on the SAME point estimates (local.llama-2-7b-chat.est = 0.33370645783382713, ci [0.24105888744782053, 0.6556227800498913]; local.llama-3.1-8b-instruct.est = 0.24890331848503333, ci [0.17756093162076295, 0.49720354707729486]), and \u00a74.7 of the same build prints them while stating the rule \u00a74.1 breaks: \"numerator and denominator move together, so they have to be resampled together\". \u00a76.1 states the same rule globally (\"Every contrast in this paper resamples name pairs\"). I recomputed the pair bootstrap from the raw data/delta_stability/*.jsonl with an independent seed (777, 4000 draws) and reproduced 0.3337 [0.241, 0.645] and 0.2489 [0.178, 0.499], so the wider intervals are real. The build therefore prints two conflicting 95 % intervals for the identical quantity, and the \u00a74.1 one understates the upper end by roughly 35 % (0.42 vs 0.66) and 36 % (0.32 vs 0.50). Kept at minor because both conclusions \u00a74.1 draws from it - dispersion large relative to effect, not enough to rank models - hold under either interval.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, change the \u00a74.1 ratio-interval source at lines 1504-1507 from dispersion_uncertainty.json's _du[m]['ratio_ci'] to ratio_intervals.json's rint['local'][m]['ci'] (already loaded under the key \"rint\" at line 61), so \u00a74.1 prints \"0.33 [0.24, 0.66]; 0.25 [0.18, 0.50]\" - the same numbers \u00a74.7 prints. Equivalently minimal: delete the clause ' and the dispersion-to-effect ratios the argument rests on become ' plus the join at 1504-1507, leaving \u00a74.1 with only the between-wording SD interval (69 % to 78 %), which resampling wordings does legitimately support, and let \u00a74.7 carry the ratio interval. Either edit is confined to lines 1503-1507; no artifact needs regenerating.",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json;d=json.load(open(r'paper-a/data/delta_stability/dispersion_uncertainty.json'))['models'];r=json.load(open(r'paper-a/data/reference/ratio_intervals.json'))['local'];[print(m, 'point', round(d[m]['ratio'],4), '| 4.1 wording-only CI', [round(x,3) for x in d[m]['ratio_ci']], '| 4.7 pair-cluster CI', [round(x,3) for x in r[m]['ci']], '| relwidth SD vs ratio', round(d[m]['sd_ci_relative_width'],10), round((d[m]['ratio_ci'][1]-d[m]['ratio_ci'][0])/d[m]['ratio'],10)) for m in ('llama-2-7b-chat','llama-3.1-8b-instruct')]\"  # then see analyze_dispersion_uncertainty.py:79,86 and build_paper_v3.py:1506 vs build_paper_v3.py:2892-2903",
      "severity": "minor",
      "root_cause": "interval-omits-denominator-uncertainty"
    },
    {
      "id": "STAT-6",
      "verdict": "CONFIRMED",
      "reason": "The sentence is still in the current build (fresh PyMuPDF extract, p.16; emitted at build_paper_v3.py:3109-3111): \"The two arms are not distinguishable on a frontier model either, which is the finding that makes this dispersion a property of the instrument rather than of meaning.\" The only evidence behind it is two bare point SDs. data/frontier/frontier_margin_analysis.json -> models[*].arms contains exactly {n_wordings: 6, sd, mean} per arm and nothing else; analyze_frontier_margin.py:166-173 computes them as np.std(vals, ddof=1) over six per-variant estimates, with no bootstrap, no joint fit, no difference. Table 13 prints them as \"nul 0.105 sem 0.108\" with no interval, and frontier_noise_floor.json has no arm contrast. The paper's identical local claim IS supported: data/delta_stability/arm_contrast.json carries difference_null_minus_semantic intervals and prob_null_exceeds_semantic (0.1227 to 0.8347, the paper's \"0.12 to 0.83\"), and the \u00a74.1 text says the joint fit exists \"so that the difference has a posterior rather than being compared by eye\" \u2014 which is exactly what the frontier sentence does, while the word \"either\" borrows the local claim's footing. The \u00a74.6 second-task passage, on the same kind of evidence, correctly stops at description. The conclusion is not wrong \u2014 a name-pair cluster bootstrap I ran on the raw JSONL gives sd_null - sd_semantic of -0.0033 [-0.085, +0.109] (gpt-4o-mini), +0.0328 [-0.029, +0.094] (gpt-4o), -0.0139 [-0.112, +0.084] (gpt-4.1-mini), all covering zero \u2014 but neither that interval nor any test appears in the paper or the artifacts, so the claim as printed outruns what is reported. Minor, as filed.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, lines 3109-3111, replace the equivalence assertion \"The two arms are not distinguishable on a frontier model either, which is the finding that makes this dispersion a property of the instrument rather than of meaning.\" with a description plus the missing-fit caveat, e.g. \"This panel carries no joint two-sigma fit, so unlike \u00a74.1 the arms are compared by point estimate only: what we claim here is that null edits move the estimate by an amount of the same order as paraphrases, not that the two arms are indistinguishable.\" (The stronger claim can instead be earned by running the arm_contrast joint model on the frontier and second-task panels and reporting difference_null_minus_semantic and prob_null_exceeds_semantic as data/delta_stability/arm_contrast.json already does for the four open-weight checkpoints.)",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json; a=json.load(open(r'paper-a/data/frontier/frontier_margin_analysis.json')); print('FRONTIER arms:', {m: v.get('arms') for m,v in a['models'].items() if v.get('arms')}); b=json.load(open(r'paper-a/data/delta_stability/arm_contrast.json')); print('LOCAL arm_contrast fields:', sorted(b['llama-2-7b-chat']['all']))\"  # then see build_paper_v3.py:3109-3111",
      "root_cause": "claim-outruns-evidence",
      "severity": "minor"
    },
    {
      "id": "STAT-10",
      "verdict": "CONFIRMED",
      "reason": "figures_budget.py:126 still writes `ci=[abs(x) for x in sorted(_mm[\"ci\"], key=abs)]`, and the deposited data/delta_stability/dispersion_budget.json on disk carries the result: llama-2-7b-chat true CI [-0.0438,+0.0064] deposited as [0.0064,0.0438]; mistral-v0.1 [-0.0271,+0.0765] deposited as [0.0271,0.0765]; mistral-v0.3 [-0.1877,+0.1260] deposited as [0.1260,0.1877] \u2014 three intervals that straddle zero deposited as intervals that exclude it, and on both Mistral checkpoints the stored `value` (0.0269, 0.0315) falls outside its own stored interval. The code's own comment at figures_budget.py:118-121 says the interval is carried into the artifact \"so the figure's consumers can see that this component ... is not separable from zero\", which the abs-and-sort defeats. The quoted deposit sentence is still in the current PDF (\"the analysis artifact each number is interpolated from\", Data and code availability, p.28 of paper_instrument_validity_v3.pdf), so the artifact is covered by the deposit claim. Severity stays minor: the paper's prose is unaffected \u2014 build_paper_v3.py:2174-2176 prints the signed interval from name_length_effect.json and the PDF correctly reads \"-0.0914 with a 95 % interval of [-0.219, -0.009] and p = 0.078\" \u2014 and the artifact also carries `p` and `separable_from_zero: false`, which are correct.",
      "minimal_fix": "In paper-a/src/figures_budget.py line 126, replace `ci=[abs(x) for x in sorted(_mm[\"ci\"], key=abs)],` with `ci=list(_mm[\"ci\"]), est_signed=_mm[\"est\"],` (keep `value=abs(_mm[\"est\"])` on line 125 as the bar height, so the plot is unchanged), then re-run `C:/research-toolchain/venv/Scripts/python.exe paper-a/src/figures_budget.py` to regenerate paper-a/data/delta_stability/dispersion_budget.json. Safe: no consumer reads the `ci` field \u2014 build_paper_v3.py reads only over_beta/kind/separable_from_zero/beta_logodds (lines 455-472, 524-526, 2146-2149) and tests/test_scales_and_intervals.py:44 asserts only on the noise-floor ratio.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import json;b=json.load(open(r'paper-a/data/delta_stability/dispersion_budget.json'));n=json.load(open(r'paper-a/data/instrument/name_length_effect.json'));[print(m,'dep',b['models'][m]['components']['token matching']['ci'],'true',n[m]['token_matched_first_name_clustered']['matched_minus_all']['ci']) for m in b['models']]\"  # and paper-a/src/figures_budget.py:126",
      "severity": "minor",
      "root_cause": "sign-stripped-interval"
    },
    {
      "id": "R5",
      "verdict": "CONFIRMED",
      "reason": "The current PDF still carries the \u00a74.5 parenthetical \"(\u00a74.1 puts the interval on it at about \u00b135 %)\" (fresh PyMuPDF extraction, text lines 1673-1676), and it comes from a hand-typed literal at build_paper_v3.py:2480 (the finding's line 2464 is stale). \u00a74.1 does not say \u00b135 %; it says the interval is \"69 % to 78 % of the point estimate\", interpolated from summary.min_relative_width/max_relative_width at build_paper_v3.py:1501-1502 \u2014 a relative WIDTH, not a half-width. Reading models[*].sd_ps and sd_ci from data/delta_stability/dispersion_uncertainty.json, and re-deriving them from study2_v2.json per_variant with the same seed (20260801, 20,000 resamples, exact match to 5 dp), the interval is strongly asymmetric on every model: lower bound \u221242.7 %, \u221242.3 %, \u221241.8 %, \u221251.6 % below the point estimate; upper bound +26.4 %, +27.9 %, +28.0 %, +26.6 % above. So \u00b135 % brackets nothing correctly \u2014 no model's upper bound reaches +35 % and every model's lower bound falls past \u221235 %. It survives only as a loose gloss on half the width (34.5 %\u201339.1 %), and even then it quotes the minimum while the \u00b1 notation asserts a symmetry the bootstrap does not have. The finding's own proposed figures (\u221242 % to \u221252 %, +26 % to +28 %) are correct as rounded.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, lines 2479-2480, drop the hand-typed \u00b1 and repeat \u00a74.1's own interpolated width (disp_unc is already in scope in main(), loaded at line 305). Replace the two string fragments `\"deviation with eleven degrees of freedom (\u00a74.1 puts the \"` / `\"interval on it at about \u00b135 %), and three such estimates \"` with an f-string reading `f\"deviation with eleven degrees of freedom (\u00a74.1 puts the interval on it at {pct(disp_unc['summary']['min_relative_width'], 0)} to {pct(disp_unc['summary']['max_relative_width'], 0)} of the point estimate), and three such estimates \"`. This renders \"69 % to 78 % of the point estimate\", matches \u00a74.1 verbatim, removes the false symmetry, and ties the number to the artifact. If the author prefers to state the asymmetry explicitly instead, the correct interpolated values are min/max of sd_ci[0]/sd_ps and sd_ci[1]/sd_ps: \u221242 % to \u221252 % below and +26 % to +28 % above.",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json;d=json.load(open(r'paper-a/data/delta_stability/dispersion_uncertainty.json'))['models'];[print(m, 'lower %+.1f%%'%((v['sd_ci'][0]/v['sd_ps']-1)*100), 'upper %+.1f%%'%((v['sd_ci'][1]/v['sd_ps']-1)*100), 'width %.1f%%'%(v['sd_ci_relative_width']*100)) for m,v in d.items()]\"  # then compare with build_paper_v3.py:2480 and PDF \u00a74.5",
      "severity": "minor",
      "root_cause": "hand-typed-stat-misstates-artifact"
    },
    {
      "id": "R6",
      "verdict": "CONFIRMED",
      "reason": "The claim is present verbatim in the current build (p.3, \"An auditable pipeline. Every number in this paper is interpolated from an artifact on disk by the script that typesets it.\", emitted at build_paper_v3.py:1057-1058, and reinforced at 4702-4708 \"the build fails rather than printing a number it cannot source\"). Three load-bearing numbers are hand-typed literals in that same script while the loaded artifacts hold them: (1) \"168 mechanism contrasts\" at lines 3658, 3667, 3678, 4415, 4424 \u2014 mech_panel_analysis.json stores bh_family_size = 168 under every one of the 12 model x mode entries, and the string \"bh_family_size\" never appears in the builder; (2) \"on 284 replicate cells\" at line 871 \u2014 second_task_analysis.json summary.n_replicate_cells = 284 (recomputed from the eight per-cell noise_floor blocks: 32 + 36x7 = 284), and the builder already interpolates that exact field at line 2653; (3) \"none of 504 cells on three of the four models, and on 212 of 504 on the fourth\" at lines 3369-3370 \u2014 noise_floor.json gives cross_session.n = 504 for all four models with n_identical 0/0/0/212 (mistral-7b-instruct-v0.3), and the builder already interpolates the minimum of those same fields at lines 3284-3287. All three artifacts (second, noise, mech) are loaded in main() at lines 304, 272, 273, so every literal site has the artifact in scope. audit_consistency.py contains no reference to bh_family_size, n_replicate_cells, cross_session, or the digits 168/284/212/504, so nothing checks the literals against the artifacts. The values are currently correct, which is why this is minor rather than a factual error \u2014 the defect is that the universal auditability claim outruns the implementation and the literals will drift silently.",
      "minimal_fix": "Interpolate the three values from artifacts already loaded in main(). Add three guarded derived constants near the \"quantities used in more than one section\" block (build_paper_v3.py ~line 311-330): _n_rep2 = ((second or {}).get(\"summary\") or {}).get(\"n_replicate_cells\"); _bhN = next((md[\"bh_family_size\"] for mv in (mech or {}).values() if isinstance(mv, dict) for md in mv.values() if isinstance(md, dict) and \"bh_family_size\" in md), None); _cs = [noise[m][\"cross_session\"] for m in models if noise and m in noise and \"cross_session\" in noise[m]]. Then substitute, each behind an \"if value else omit-the-clause\" guard matching the file's existing pattern: line 871 \"284\" -> f\"{_n_rep2}\"; lines 3369-3370 \"none of 504 ... 212 of 504 on the fourth\" -> counts derived from _cs (n = _cs[0][\"n\"], number of models with n_identical == 0, and the nonzero n_identical); lines 3658, 3667, 3678, 4415, 4424 \"168\" -> f\"{_bhN}\". If the author prefers one edit over seven, the alternative is to weaken the claim at build_paper_v3.py:1057-1058 and 4705-4708 from \"Every number\" to a scoped statement (e.g. \"every estimate, interval and count reported in the tables and figures\"), but that also touches two sites and gives up the paper's strongest reproducibility claim.",
      "evidence_command": "grep -n \"168 mechanism\\|on 284 replicate cells\\|212 of 504\" \"paper-a/src/build_paper_v3.py\"  # literals at 871, 3369-3370, 3658, 3667, 3678, 4415, 4424 -- then: \"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json; m=json.load(open(r'paper-a/data/mechanism_panel/mech_panel_analysis.json')); s=json.load(open(r'paper-a/data/second_task/second_task_analysis.json')); n=json.load(open(r'paper-a/data/delta_stability/noise_floor.json')); print(m['llama-3.1-8b-instruct']['chat']['bh_family_size'], s['summary']['n_replicate_cells'], [(k, v['cross_session']['n_identical'], v['cross_session']['n']) for k,v in n.items()])\"",
      "root_cause": "claim-outruns-implementation",
      "severity": "minor"
    },
    {
      "id": "table3-saturated-denominator",
      "verdict": "CONFIRMED",
      "reason": "The current build (fresh PyMuPDF extract of paper_instrument_validity_v3.pdf, built Aug 2 17:32) still carries the Table 3 caption verbatim: \"The final column is the fraction of cells where the model's preference is saturated beyond 0.99 or below 0.01\" (build_paper_v3.py:1474). The underlying quantity is arm-level, not cell-level: src/effectsize.py:254-256 computes saturated_frac = np.mean([... for r in rows for s in (\"white\",\"black\")]), i.e. two entries per matched-pair row. study2_v2.json reports n=432 per model with saturated_frac 0.06481481 / 0.43981481 / 0.0 / 0.99652778. Recomputing from the raw JSONL (432 records x 2 arms = 864 scorings) reproduces the printed 6.5 / 44.0 / 0.0 / 99.7 % exactly (56, 380, 0, 861 out of 864). Neither cell-level definition matches: both-arms-saturated gives 6.0 / 40.3 / 0.0 / 99.3 %, either-arm gives 6.9 / 47.7 / 0.0 / 100.0 %. Decisive check: 0.9965277... x 432 = 430.5, a non-integer, so 99.7 % cannot be a fraction of 432 cells under any definition. The paper's own vocabulary fixes \"cell\" as a matched pair \u2014 the current text says \"giving 432 matched pairs per model\", \"how many of the 432 design cells the top-100 window resolved\", \"how many of the 432 design cells the top-20 window could form a margin for\", \"formed on 1 of 432 cells\", \"Across 284 such replicate cells\" \u2014 and the paper never mentions 864 nor defines the saturation column as per-r\u00e9sum\u00e9, so a reader applying the paper's own definition of \"cell\" cannot reproduce the column. The same mislabel is repeated in \u00a76.2 (build_paper_v3.py:3538, \"the fraction of saturated cells in Table 3 ranges from 0.0 % to 99.7 %\").",
      "minimal_fix": "In paper-a/src/build_paper_v3.py line 1474, replace the string \"column is the fraction of cells where the model's \" with \"column is the fraction of resume scorings (two per matched pair, 864 per model) on which the model's \" (keeping the following line \"preference is saturated beyond 0.99 or below 0.01, which is \" unchanged). Companion edit so the body text does not re-introduce the wrong unit: line 3538, change \"the fraction of saturated cells in \" to \"the fraction of saturated resume scorings in \". No numbers change \u2014 6.5 / 44.0 / 0.0 / 99.7 % are already correct over 864.",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json,math;p=r'paper-a/data/delta_stability/delta_mistral-7b-instruct-v0.3.jsonl';rows=[json.loads(l) for l in open(p,encoding='utf-8') if l.strip()];f=lambda x:1/(1+math.exp(-x));a=[f(r[s+'_margin'])>0.99 or f(r[s+'_margin'])<0.01 for r in rows for s in ('white','black')];print('rows',len(rows),'arms',len(a),'sat',sum(a),'frac',sum(a)/len(a),'x432=',sum(a)/len(a)*432)\"  # prints rows 432 arms 864 sat 861 frac 0.9965277... x432= 430.5 (non-integer). Caption source: build_paper_v3.py:1474; artifact field: data/delta_stability/study2_v2.json -> [model].overall.saturated_frac; producer: src/effectsize.py:254",
      "severity": "minor",
      "root_cause": "unit-mismatch"
    },
    {
      "id": "CF-10",
      "verdict": "CONFIRMED",
      "reason": "Fresh extraction of paper_instrument_validity_v3.pdf still contains the unqualified abstract sentence \"we do not convert our movement to that scale, and \u00a76.2 and \u00a78 say why\" (source: build_paper_v3.py:787-788). It is contradicted by the current build's own Figure 5 (fig5_variance_components, page 20), whose x-axis reads \"Between-wording standard deviation of the demographic effect (percentage points, upper bound at the p = 0.5 slope \u2014 see \u00a76.2)\" and whose caption reads \"Percentage points are computed at the logistic's maximum slope and are therefore upper bounds (\u00a76.2)\". The plotted quantities are the artifact fields sigma_variant_pp_max and beta_pp_max in data/delta_stability/variance_components.json, produced by multiplying the paper's own log-odds components by PP_PER_LOGIT_MAX = 25.0 (fit_variance_components.py:184-186, figures_study2.py:133,143,157-159). Median sigma_variant_pp_max runs 0.40 to 2.31 pp, i.e. the same range as the published callback gaps the abstract cites (median 0.64 pp, 11 of 14 below 1.1), so a reader is invited to make exactly the comparison the abstract disclaims. \u00a76.2 does not \"say why\" the paper declines to convert; it states the opposite policy \u2014 \"percentage points only where a per-cell probability-scale interval can be computed. Where a quantity has no single operating point attached \u2014 a variance component, for instance \u2014 any percentage-point figure is reported as an upper bound and named as one.\" The finding's secondary claim is also accurate: the Figure 8 caption text \"Our own effect is on a log-odds scale and is deliberately not plotted on the left axis\" is live at figures_literature.py:235-237, though that narrower wording is literally true, so the defect is confined to the abstract. \u00a78's shortlist bridge (6.2/2.9/3.7/2.7 points vs 0.64) is already explicitly withdrawn as a quantitative comparison and is not the basis for this verdict.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py line 787, replace the string \"fall below 1.1; we do not convert our movement to that scale, and \" with \"fall below 1.1; we convert our movement to that scale only as an explicitly labelled upper bound at the logistic's maximum slope, never as a figure comparable to a published gap, and \" (leaving line 788 \"\u00a76.2 and \u00a78 say why. \" unchanged). Do not hard-code a \"Figure 5\" cross-reference in the abstract \u2014 the abstract string is assembled before the figure registry is populated, so a literal number would be one more thing to drift.",
      "evidence_command": "C:/research-toolchain/venv/Scripts/python.exe -c \"import fitz; d=fitz.open(r'paper-a/figures/paper_instrument_validity_v3.pdf'); t=''.join(p.get_text() for p in d); i=t.find('we do not convert our movement'); print(repr(t[i-180:i+120])); print('---FIG5---'); print(fitz.open(r'paper-a/figures/fig5_variance_components.pdf')[0].get_text())\"  # then see build_paper_v3.py:787 and figures_study2.py:157-159",
      "root_cause": "abstract-overstates-body",
      "severity": "minor"
    },
    {
      "id": "RW-9",
      "verdict": "CONFIRMED",
      "reason": "The clause is verbatim in the current build (fresh PyMuPDF text of figures/paper_instrument_validity_v3.pdf, \u00a710.3; emitted at src/build_paper_v3.py:4548-4551): \"Bertrand and Mullainathan tested the socioeconomic channel on their own data and could not reject it cleanly either. We test it and cannot resolve it\". That is wrong on both halves of the finding. (1) B&M printed a directional conclusion, not an unresolved one: the abstract says \"We also find little evidence that employers are inferring social class from the names\" (lit/text/bm2004.txt line 10), and p. 1009 says \"The social background hypothesis predicts a positive correlation. Yet, for all four categories, we find the exact opposite... In summary, this test suggests little evidence that social background drives the measured race gap.\" (2) The signs run opposite. Recomputing Spearman(callback_pct, mother_education_pct) within each race-gender cell from the repo's own transcription (paper-a/data/reference/bm2004_names.json) gives white_female -0.357, black_female -0.377, white_male -0.025, black_male -0.595 (n=8; Tremayne has no mother-education value, matching B&M's own drop) - reproducing the printed -0.318/-0.383/-0.0251/-0.595 already recorded in lit/notes/bertrand_mullainathan_2004_aer.md. This paper's four, read from paper-a/data/names/construct_validity.json field models[*].q3_ses.pooled_within_race.rho, are +0.017, +0.070, +0.315, +0.329 - all positive. The word \"either\" asserts a parity with B&M that the two studies' evidence does not have: B&M's data leans against the socioeconomic channel and they said so, while this paper's leans toward it (its own \u00a74.3 says the result \"points AGAINST us\"). Round-5 triage already marked this quote verdict \"verbatim\", coverage 1.0, and the current build confirms it was never edited.",
      "minimal_fix": "In paper-a/src/build_paper_v3.py, lines 4550-4551, replace the string fragments \"tested the socioeconomic channel on their own data and could not \" / \"reject it cleanly either. \" with: \"tested the socioeconomic channel on their own data and found little \" \"evidence for it \u2014 their four within-race-gender rank correlations \" \"between callback rate and mother's education all run negative, the \" \"opposite sign to the four we report. \" (Leave the rest of the paragraph, including \"We test it and cannot resolve it\", unchanged; delete only the word \"either\".)",
      "evidence_command": "\"C:/research-toolchain/venv/Scripts/python.exe\" -c \"import json; from scipy.stats import spearmanr; d=json.load(open(r'paper-a/data/reference/bm2004_names.json',encoding='utf-8')); [print(c, spearmanr([r['callback_pct'] for r in v if r.get('mother_education_pct') is not None],[r['mother_education_pct'] for r in v if r.get('mother_education_pct') is not None])) for c,v in d['first_names'].items()]; e=json.load(open(r'paper-a/data/names/construct_validity.json',encoding='utf-8')); [print(m, e['models'][m]['q3_ses']['pooled_within_race']['rho']) for m in e['models']]\" ; grep -n \"little evidence that employers are inferring social class\" \"lit/text/bm2004.txt\" ; grep -n \"this test suggests little evidence that social back\" \"lit/text/bm2004.txt\" ; sed -n '4548,4551p' \"paper-a/src/build_paper_v3.py\"",
      "root_cause": "cited-source-mischaracterized",
      "severity": "minor"
    }
  ],
  "needs_human": [],
  "summary": "**1. Unqualified scope \u2014 the prose quantifier is wider than the model set the number was computed over** (STAT-3, serving-bound-8.7, SC-03, STAT-2, batching-every-checkpoint, SC-04, CF-7, decision-boundary-premise, SC-05, STAT-6/occupation, A8, RW-2)\n\nIn `paper-a\\src\\build_paper_v3.py`, make every subset-filtered statistic return its denominator alongside its value and force the prose to interpolate it: `_serving_bound` (549\u2013573, which skips `_m not in survives` at 561) yields `(0.087, [llama-2-7b-chat, llama-3.1-8b-instruct])` for lines 3399/4287/4663; the Table 15 caption at 3344 names `cache_residual.json`'s 2 models against `replicate_analysis.json`'s 4 that produced 47.9 %/93.8 %; 1041 reads the 4 keys of `replicate_analysis.json`, 968 reads `noise_vs_probability.json` `models[*].n_in_band` and `summary.n_models_with_no_cell_in_band`, 1223 reads `corpus_size.json` `n_studies_matched_pair` (9) not `n_studies` (11), 4080 reads the 6-of-13 non-null `negative_check` fields in `reporting_practice_matrix.json`, and 2528/4438 de-conjoin the occupation counts from `occupation_analysis.json` `ps` / `ps_sd_across_wordings`. Then add one rule to `paper-a/src/audit_consistency.py` that fails the build when a string containing \"every / all / no \u2026 panel / checkpoint / audit / cells\" interpolates a value derived from a filtered model list, which is the mechanism that let all twelve survive.\n\n**2. A correction landed at one site and the stale copies were left standing** (token-direction-contradiction, ratio-ranking-resurfaces, A5, CF-10, RW-4, R4)\n\nCompute each contested verdict once as a named constant near the top of `build_paper_v3.py` and interpolate it at every site \u2014 the builder already defines `_mask_interp` / `_mask_interp_away` (383\u2013387) and never references them while the flat direction claim survives at 1016 and 2338; the same binding fixes \u00a710.2's panel ranking at 4483 against `ratio_intervals.json` `comparison.ranking_is_supported = false`, the abstract's name-length verdict at 724 against `name_length_effect.json`'s three flags (`difference_significant`, `_by_ci`, `_by_relabel`), the abstract's \"we do not convert\" at 787 against `variance_components.json` `sigma_variant_pp_max` which Figure 5 plots, and \u00a72's Sclar counts at 1136\u20131141 against the already-corrected \u00a77 at 3890\u20133913. Pair it with an audit rule that no claim may be stated affirmatively when the artifact flag behind it (`ranking_is_supported`, `difference_significant`, `interpretable`) is `false`, so a withdrawal in one section cannot coexist with an assertion in another.\n\n**3. Claims about other people's work asserted in prose instead of read from the literature artifacts** (RW-3, RW-5, RW-6, name-variance-priority, RW-9)\n\nRequire every literature-facing sentence to interpolate a cell of `paper-a/data/reference/reporting_practice_matrix.json` or a quoted line of `research/lit/text/*`, and add the cells these five claims need: a `manipulation_check` cell carrying Tan et al.'s recoverability probe (macro-F1 0.925\u20130.997), a `name_variance_reported` cell carrying Armstrong et al.'s printed Name-intercept variance 0.016 / SD 0.127, a prefix-caching cell for He 2025 lines 297\u2013308, an explicit screening record naming Haim/Salinas/Nyarko (2024) as excluded, and the four negative within-cell rank correlations recomputable from `paper-a/data/reference/bm2004_names.json`. With those fields in place the sites at 1002, 3309, 4074, 4264, 4546 and 4550 print scoped counts and credited precedents instead of universals (\"first\", \"neither account covers\", \"no audit we surveyed\", \"either\"), and the panel's inclusion criterion at 4074 is restated topically rather than as availability.\n\n**4. Caption or label names a different estimator, unit, or contrast than the one that produced the printed number** (A4, table6-caption-posterior-medians, A3, table3-saturated-denominator, A6, R5, STAT-6/frontier)\n\nHave each analysis script emit an `_estimator` description string next to the value it computes and have `build_paper_v3.py` interpolate that string into the caption instead of hand-typing one: `fit_name_variance.py:451` declares \"SD over 4,000 cluster-bootstrap draws \u00f7 posterior median\" for Table 6 (1811\u20131812), `effectsize.py:254` declares \"fraction of 864 r\u00e9sum\u00e9 scorings, two per matched pair\" for `study2_v2.json` `saturated_frac` (1474, 3538), `build_paper_v3.py:1251` declares \"mean of per-probe ratios, not the quotient of the two column means\" (1288\u20131289), `analyze_name_length.py` keeps `n_draws_degenerate` (\"draws containing no matched cluster\", 602/20,000) textually separate from `same_cluster_draw_probability` (0.111) at 2197\u20132201, and 2480's hand-typed \"\u00b135 %\" is replaced by `dispersion_uncertainty.json` `summary.min/max_relative_width`. Where no estimator exists the sentence must be demoted rather than relabelled \u2014 `frontier_margin_analysis.json` `models[*].arms` holds only `n_wordings`/`sd`/`mean`, so 3109\u20133111's \"not distinguishable \u2026 either\" becomes a point-estimate description until an `arm_contrast`-style joint fit is run on that panel.\n\n**5. The weaker of two specifications already sitting on disk is the one printed** (STAT-5, STAT-1, STAT-3/two-intervals-one-estimand)\n\nRepoint \u00a74.1's two reads at the branch the analysis code itself prefers: `build_paper_v3.py:1532` should print `arm_contrast.json[m][\"dropN1\"][\"prob_null_exceeds_semantic\"]` (0.05\u20130.88) beside `[\"all\"]` (0.12\u20130.83), since `fit_arm_contrast.py:35\u201338` states N1 is byte-identical to S1 and biases the null arm toward the paper's conclusion; and 1503\u20131507 should read `ratio_intervals.json` `local[m].ci` (0.33 [0.24, 0.66]; 0.25 [0.18, 0.50]) rather than `dispersion_uncertainty.json` `ratio_ci`, whose denominator is fixed outside the bootstrap loop at `analyze_dispersion_uncertainty.py:79,86` and which contradicts \u00a74.7's printing of the same estimand. Adopting the rule \u00a74.7 and \u00a76.1 already state \u2014 resample name pairs so numerator and denominator move together, and print both branches of any two-way specification \u2014 leaves no computed artifact key unread, which is the invariant an audit can enforce.\n\n**6. The pipeline's self-description outruns the pipeline** (R3, R6)\n\nBind the three remaining hardcoded figures to the artifacts already loaded in `main()` \u2014 168 to `mech_panel_analysis.json[*][*].bh_family_size` (3658, 3667, 3678, 4415, 4424), 284 to `second_task_analysis.json` `summary.n_replicate_cells` (871), and 504/212 to `noise_floor.json[m].cross_session.n` / `n_identical` (3369\u20133370) \u2014 and rewrite 4707\u20134708 to state the drop-and-warn behaviour that the module docstring (14\u201317) and `main()`'s `return 0` (4825\u20134829) actually implement. Then make the claim self-enforcing by having `audit_consistency.py` flag any three-or-more-digit integer literal inside a prose string, since the absence of that check is the only reason a paper claiming every number is interpolated shipped three that were not.\n\n**7. A transform applied for presentation breaks the comparison it feeds** (R2, STAT-10)\n\n`paper-a/src/analyze_length_prediction.py:125` must average `dl` over `common` (all 48 pairs) rather than `dropped`, because the observed side is `matched_minus_all` \u2014 a matched-versus-all-48 contrast \u2014 which rescales every predicted-shift cell in Table 10 by `n_dropped/48` and moves obs/pred from 0.47\u20132.20 to 0.71\u20133.30 and the headline 1.07 to 1.42; and `paper-a/src/figures_budget.py:126` must deposit `ci=list(_mm[\"ci\"])` instead of `[abs(x) for x in sorted(...)]`, which currently publishes three zero-straddling intervals in `dispersion_budget.json` as intervals excluding zero, two of them not even containing their own stored `value`. A single test asserting that each stored `est` lies inside its stored `ci`, and that any predicted/observed pair shares one contrast definition, closes both and would have caught them at deposit time."
}